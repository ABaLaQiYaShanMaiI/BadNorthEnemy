# Black Spearman v1.3 —— 困惑清单与调试记录

> 撰写日期：2026-08-14
> 状态：**技能效果实现成功（定稿）** —— P0-1/P0-2/P1-3 已解决，可躲高收益达成。
> 当前基底：`Viking_SwordShield`（保留真实盾牌美术）｜ 当前 DLL 已部署（大脑目标触发 + AgentEnumerators 命中 + 终点爆发 + 10s 冷却 + 近战刺击抽动修复 + 盾牌格挡）

---

## 0. 当前技能参数快照（定稿）

| 参数 | 值 | 说明 |
|---|---|---|
| `DetectionRadius` | 6.0m | 扫描兜底范围（优先取 Swordsman 大脑目标） |
| `ChargeSpeed` | 5.0m/s | 冲刺速度 |
| `ChargeOvershoot` | 1.5m | 穿透余量（冲过锁定格 1.5m） |
| `RetreatDistance` | 0.6m | 后退距离 |
| `WindUpDuration` | 0.5s | 起手举矛 |
| `CooldownTime` | 10s | 冷却（2026-08-15 用户指定延长：冲锋更稀有、更像"技能"） |
| `StabDamage/Knockback/Stun/Launch` | 3 / 9 / 10 / 8 | 基础伤害/击退/眩晕/撞飞（撞飞只在抵达爆发） |
| `HitRadius` | 0.5m | 单矛线宽（沿途命中） |
| `ArrivalBurstRadius` | 1.2m | 终点爆发半径 |
| `EnergyDecayPerHit` | 0.8 | 每命中能量衰减（多段递减） |

状态机：`Idle → WindUp → Charging(穿透) → Retreat(后退0.6m) → Cooldown(10s) → Idle`

---

## 1. 待解决困惑（按优先级）

### 🔴 P2-7：普通攻击"挥剑动画+跳扑" —— 根因实锤（2026-08-15 三轮修复完成）

**现象**：黑矛兵普通攻击仍是"用长矛执行剑的劈砍"（挥剑动画+跳扑），v1.3 长矛穿刺（跳过 Attack 原版）看似没生效。

**日志没暴露问题的原因（三处）**：
1. **根因被埋**：最新会话日志有一行 `[ERROR] [BS v1.3] Awake 初始化异常: System.TypeLoadException: Could not load type 'System.Func`2' from assembly 'mscorlib, Version=4.0.0.0'`——它是唯一信号，但只有一行，被 67 万行日志淹没；且 mod 其余功能（生成/染色/冲锋/刺击表现）继续运行，让人误以为 Patch 都生效了。
2. **`PatchSwordsman` 整体 JIT 崩溃**：旧代码 `typeof(Func<Agent,bool>)`（net472 编译 → `[mscorlib 4.0.0.0]System.Func`2`）在 JIT 编译 PatchSwordsman 方法体时抛 TypeLoadException，**range/GetAttack/Attack/AttackUpdate 四条 Patch 全部静默失效**。启动日志里因此永远只有 `LevelNode.Setup` 一条成功。
3. **日志信号被误读**：`body=hop` 是 `hopping.active`（容器状态，几乎恒 true）而非跳扑；`anim=hash=81563449` 无人能读懂（不知道是 Idle/Run/Attack）。

**⚠️ 更深一层的病根（第二轮实测 2026-08-15 21:0x）**：游戏本体程序集引用 `mscorlib 4.0.0.0`（Unity 2018.4 .NET 4.x 配置），运行时 mscorlib 也是 4.0.0.0——**但 Unity Mono 的 mscorlib 里 `System.Action`/`System.Func` 是 type-forward（真正实现移到 System.Core）**。因此：
- **游戏自己**（Unity 编译器）引用它们时走 `[System.Core 4.0.0.0]` → 运行时正常；
- **net472 编译的 mod** 引用它们时走 `[mscorlib 4.0.0.0]` → MonoMod 反射枚举 mod 方法签名时抛 `Could not load type 'System.Action' from mscorlib 4.0.0.0`（见 On.Landing.add_Spawn 堆栈）。
- 结论：**mod 内禁止直接引用 `System.Action`/`System.Func`（含一切会隐式生成它们的写法：lambda 转 Action、List.ForEach、链式委托）**。net35 目标框架也不行（游戏引用 mscorlib 4.0，net35 编译直接 MSB3258 拒绝）。正确姿势 = net472 + 自定义委托（如 `delegate void PatchJob()`）代替 Action。
- **⚠️ 第三轮雷区（2026-08-15 21:20 实测，op_Equality）**：第二轮修掉 Action 后，启动日志仍**全部 FAIL**：`MissingMethodException: Method not found: 'System.Reflection.MethodInfo.op_Equality'` / `'System.Reflection.PropertyInfo.op_Inequality'`。根因：TryPatch 的 lambda 对反射对象（`MethodInfo`/`PropertyInfo`）用 `==`/`!=` null——net472 编译产物引用 `System.Reflection.MethodInfo::op_Equality` / `PropertyInfo::op_Inequality`（.NET 4.0 的 mscorlib 才声明这些运算符），**Unity 运行时 mscorlib 没有** → 执行到 lambda 即 MissingMethodException，6 条 Patch 全 FAIL。旧代码与 `SwordRemover.cs` 注释早已立下铁律（"Mono 2.0 没有 FieldInfo.op_Equality，判空必须用 ReferenceEquals"），本轮新代码违反。修复：反射对象判空一律 `ReferenceEquals(x, null)`（含 `AgentPlayAnimationPrefix` 的 `m.DeclaringType != null`、`CaptureScreenshotOnce` 的 `mi != null`）；产物 Mono.Cecil 扫描确认 `System.Reflection.*` 的 op_Equality/op_Inequality **0 处**。

**证据链（日志检索）**：
- 六个旧会话（16:13/16:39/19:28/19:43/19:56/20:20）启动日志只有 `range`/`GetAttack` 两条 Patch 成功（当时源码还没有 Attack 穿刺代码）；旧构建净引用无 Action/Func → MMHOOK/Harmony 正常；
- 会话（20:42）`PatchLevelNodeSetup` 成功 → 紧接 `Awake 初始化异常 System.Func`2` → **range/GetAttack/Attack/AttackUpdate 一条都没有**；
- 会话（21:0x，修掉 Func 后）`On.Landing.add_Spawn` 抛 `System.Action` TypeLoadException（MonoMod 枚举到 mod 的 TryPatch/Action lambda）；
- 旧会话游戏内 `[近战] 攻击开始 ... range=0.09`（=原版 radius*0.7）——range Patch 失效铁证；`body=hop` + Attack 动画帧照常播放 → 原版剑逻辑 100% 在跑。

**修复（三轮，2026-08-15 本次已实施）**：
- 第一轮：删 `typeof(Func<Agent,bool>)`，改 `GetMethod("Attack", BindingFlags.Instance|Public)` 按方法名定位（Swordsman 只有一个 public Attack，无歧义）；每条 Patch 独立 `TryPatch`（单独 try/catch + ✅/❌ + 失败继续跑），启动输出 `[PATCH·总览]` 一行汇总全 OK/FAIL；
- 第二轮（Action 崩溃）：`TryPatch` 的 `Action apply` 换成 mod 自定义委托 `delegate void PatchJob()`；连刺不再调用原版 `__instance.Attack(...)`（其签名含 Func<,>），改为直接重启 `attack` 状态 + `NotifyMeleeAttackStart`；并验证产物元数据无 `System.Action`/`System.Func`/`Func`2`；
- 第三轮（op_Equality 崩溃）：所有对反射对象的 `==`/`!=` null 一律改 `ReferenceEquals`（net472 引用的 `System.Reflection.MethodInfo/PropertyInfo/Type.op_Equality` 在 Unity 运行时 mscorlib 不存在 → 每条 TryPatch 执行即 MissingMethodException）；`AgentPlayAnimationPrefix` 顺带修掉 `m.Name` 的 NRE 隐患（`ReferenceEquals(m,null)` 直接 continue）。
- 启动日志加 `[装配]` 身份（路径/大小/修改时间），防"改了源码没部署/部署错位"；
- 运行时拦截证据：`[近战·拦截] Attack() 已接管 ... range=0.69`（同时是 range Patch 活体探针）、`[近战·接管] AttackUpdate 已接管`；
- `[动画·溯源]`：Patch `Agent.PlayAnimation(int)`，黑矛兵播放 Attack/Clash 时打调用栈；
- `[近战·待机帧]`/`[近战] 攻击开始` 升级为可读信息：`animCtrl/clip 名/hash/norm` + `body=stand/step/slide/hop`（修正 hopping 先判的误导）；
- 首次刺击自动整屏截图 `BS_shot_attack_*.png`（视觉取证，可确认攻击瞬间画面）。

**验证方式（下次实测）**：
1. 启动日志应出现 `[装配] 大小=86016B`、`[PATCH] ✅ Swordsman.Attack（长矛穿刺·不播挥剑）` 与 `[PATCH·总览] ... Attack=OK AttackUpdate=OK PlayAnim=OK`（三轮修复后若仍看到 `MissingMethodException: op_Equality` 或 `Awake 初始化异常` → 直接贴首屏给作者）；
2. 战斗日志出现 `[近战·拦截] Attack() 已接管 ... range=0.69`（不再是 0.09）；
3. `[近战] 攻击开始` 的 `clip=` 应显示 Idle/Run 而非 Attack；若仍出现 Attack 且 `[动画·溯源]` 有调用栈 → 谁播的、为什么一目了然；
4. 打开 `D:\Steam\steamapps\common\BadNorth\BepInEx\plugins\BS_shot_attack_*.png` 直接看攻击瞬间画面。



### ✅ P0-1：冲锋触发不可靠 —— 已解决

**现象**：黑矛兵有时不冲锋，而是普通走路接近我方阵前。
**根因**：独立 6m 扫描与 Swordsman 大脑的战术目标不同步；且协同防重把所有目标锁光后放弃冲锋。
**修复（已实装）**：
- 触发改为**大脑目标优先**：`GetBrainTarget()` 取 `Swordsman.target` / `enemyAgent`，方向朝目标 `navPos`（Twohanded 式）；大脑无目标才退回 6m 扫描兜底。
- 协同防重**软降级**：未锁定目标优先；全部被锁则选"锁定者最少"的目标（不再锁光放弃）。
- 大脑目标也参与防重（`IsLockedByOthers`）：已被其他黑矛兵锁定则换扫描目标，避免挤同一人。
- 目标贴脸（<0.6m）不冲，交给普通攻击。

### ✅ P0-2：冲锋命中率低 —— 已解决

**现象**：矛尖扫过我方单位但没有命中。
**根因**：命中锚点用 `navPos` 算矛尖，而冲锋时 `transform` 滞后 navPos 约 1m（Body 追赶式动画）→ 命中点领先可见矛尖 1m+（"隔空命中"）。
**修复（已实装）**：
- 命中锚点改用**视觉长矛**（`_spearTransform.position`），命中=所见。
- 查询改用 `AgentEnumerators.GetStaticListRadiusSorted`（直接查游戏 Agent 列表，不受物理层/碰撞体干扰）。
- 线宽定为单矛 `HitRadius=0.5m`（原版单兵 ≈0.46m），命中范围与视觉一致。

### ✅ P1-3：协同防重过度 —— 已解决

**现象**：多个黑矛兵时，后面的黑矛兵经常找不到目标（目标全被锁定）→ 不冲锋。
**修复（已实装）**：改为"软防重 + 软降级"：
- 未锁定目标优先；全部被锁时选**锁定者最少**的目标（允许少量重叠，不再锁光）。
- 大脑目标也参与防重（`IsLockedByOthers`），被锁则换扫描目标。

### 🟡 P1-4：冲锋视觉冲击感

**现象**：用户反馈"冲锋速度不快/冲击阵营效果不明显"（已调 5m/s + 穿透 1.5m，待验证）。
**待验证**：穿透冲锋是否明显（黑矛兵冲进敌阵、穿过）。
**可调**：`ChargeSpeed` 5→6，`ChargeOvershoot` 1.5→2.5。

### 🟡 P1-5：免疫攻击边界

**现象**：冲锋+后退全程免疫攻击（attack.ignore），黑矛兵"无敌冲进无敌退走"。
**待确认**：是否保留全程免疫，或只冲锋阶段免疫（后退阶段可被打）。
**风险**：全程免疫 + 后退 0.6m + 冷却，黑矛兵可能显得"无敌且反复试探"。

### 🟢 P2-6：剑的视觉残留 —— ✅ 已验证解决（2026-08-15 二次修复）

**现象**：黑矛兵身体动画帧里带剑，去剑难度大。

**研究突破（2026-08-15，像素级定位 + 运行时诊断）**：
- 剑烘焙在 **`OnehandedXXXX` 动画帧**的**帧右侧 x≈29~38**（暗红垂直带：宽阈值 70/40/20 命中 **201 像素**、窄阈值 90/25/10 命中 132）。之前误以为在 `Swordsman0001`——那是 SwordShield 单位的帧；黑矛兵（Viking_Sword 基底）实际用 **`Onehanded` 帧**。
- 帧纹理是**共享 2048x1024 精灵图集**（`SpriteAtlasTexture-Sprites (Group 0)-2048x1024-fmt4`），必须**克隆**后按帧 rect 擦除（不能改共享资源）。
- `sprite2 = PartTex_Sword`（`PartTex_Median_BlurAlpha` 图集的 (192,0,64,126) 单元）—— 身体外观来源；`SetSprite2` 是游戏设计的换部件机制（安全）。
- 网格状态：`meshColors0=(0,96,2,0)`、`meshUV0=(0.122,0.805)` —— bSprite 交换后顶点色/UV 均正常。

**⚠️ 关键发现：`bSprite` 交换会破坏身体渲染（躯干透明）**
- 即使**只擦剑区域（201 像素）**、顶点色/UV 全部正常，只要把渲染 Sprite 换成新建的克隆 Sprite（`Sprite.Create`），`BatchedSprite` 的批量渲染就坏 → **躯干透明**（与擦除像素量无关）。
- 之前"整图集擦除 71710 像素导致身体透明"的结论需修正：**`bSprite` 交换本身才是破坏渲染的真凶**。

**✅ 修复方案（已游戏内验证：剑刃消失、躯干完好）**：不交换 bSprite，**只替换材质块的 `_MainTex` 为去剑克隆**：
- 网格 UV 本来就指向图集单元；克隆纹理与图集同尺寸 → `_MainTex` 直接采样克隆的同一单元即可渲染"去剑帧"；
- 全程不碰 `bSprite` / `sprite` / 网格 / 顶点色 → 不触发批量渲染重建。
- 实现：`SwordRemover.EnsureErasedTexture`（共享克隆 + 每帧 rect 只擦一次 + 安全阀）+ `LateUpdate` 里 `block.SetTexture("_MainTex", clone)` + `ComittBlock()`。

**🔬 二次发现（2026-08-15）**：游戏内验证后剑刃消失但**剑柄还在** → 离线统计定位：
- **剑柄/护手 = 剑刃 bbox 外侧的非红不透明像素**（灰金属色，不在 70/40/20 阈值内；如 0002 帧剑柄 x37-40）。
- 且**剑到身体左侧的帧（0030~0033、0058~0059，剑刃 x2-22）原 `x≥28` 区域会漏擦**（整剑闪回）。
- **修复 = 方向感知擦除**：① 整帧红暗像素（左右侧都擦）；② 剑外侧非红（右偏剑擦 `x≥剑右缘-2`、左偏剑擦 `x≤剑左缘+2`，bbox 上下 ±6px 带）；③ 居中剑不擦外侧。离线 59 帧全量模拟：残留=0、身体误擦=0、占比最高 15.7%（<20% 安全阀）。

**⚔️ 顺带修复："攻击逻辑仍是剑"**：`Swordsman.range` 补丁原返回 `radius×0.7×1.3≈0.118m`（贴脸）→ 改为 `radius×0.7+0.6≈0.69m`（矛长+身体半程），攻击在矛尖够到的地方触发。

**诊断工具（已内置）**：
- `去剑·运行时诊断`：帧像素 ASCII 图（`S`=窄阈值红暗 90/25/10、`s`=宽阈值红暗 70/40/20、`#`=亮色、`.`=不透明）+ **sprite2 单元 ASCII 图**（全局一次）+ 网格状态。
- `[去剑诊断]`：不透明数 / 宽窄阈值命中数 / 红暗 bbox。
- `ReadMeshState`：`meshColors0/meshUV0`（检测交换后渲染状态）。

**🔬 三次发现（2026-08-15，本轮）——剑柄残留 / 近战剑闪回 / 挥砍长矛**：
- **剑柄残留根因**：动画帧颜色实为 **UV 编码**（R/G 通道 = 采样 `PartTex_Sword` 的 UV，`_UV = color.SetB(0)-Color.white`），剑柄=剑刃基部靠身体一侧的**非红**像素（如 0030 帧剑柄 145px、0025 帧 47px），与"持剑的手/臂"重叠——旧算法外侧带擦的是**尖端侧**（背景），剑柄从未被擦到；左偏剑帧（0031~0033）剑柄在**基部右侧**也被漏。
- **近战剑闪回根因**：`EnsureErasedTexture` 逐帧"首次显示后才擦"→ 每个新帧渲染时**晚一帧**，慢放可见剑闪回。
- **修复（已实现）**：① **预擦除全部 Onehanded 帧**（`PreEraseAllOnehanded`：`Resources.FindObjectsOfTypeAll<Sprite>` 枚举同图集所有 Onehanded 帧一次性擦除，共享像素数组只上传一次 → 动画播放零闪回）；② **剑柄带改到基部身体侧**（右偏剑擦 `x∈[rx0-HiltBandPx, rx0+2]`、左偏剑擦 `x∈[rx1-2, rx1+HiltBandPx]`，bbox 上下 ±6px）；③ **安全阀只看红暗命中**（`CountRed`，剑柄带几何有界不计入，避免整帧被跳过）。
- **离线模拟（新逻辑，59 帧）**：剑柄带残留=0、身体超带误擦=0；剑柄带会连带擦掉"持剑手/臂"紧贴剑刃基部的少量像素（≤8px 宽窄条，可接受，可在游戏内复核观感）。
- **新增调参**：`HiltBandPx`（当前 5）、`ThrustDistance/Rise/Fall`（见刺击）。

**⚔️ 近战刺击（视觉"刺"而非"挥砍长矛"）**：
- 基底 Swordsman 攻击动画是挥剑帧（无法换），但剑已擦除、矛是唯一武器视觉；新增 `SpearChargeComponent.UpdateMeleeThrust()`：**`Swordsman.attack.active` 时长矛对准目标并沿矛身前向快速突刺 `ThrustDistance=0.45m`**（0.06s 刺出 / 0.28s 收回），观感为"刺"而非"挥砍长矛"。与冲锋共用 `_spearTargetRot` 插值，互不冲突。

**🔻 四次发现（2026-08-15，实测回退）——剑柄带会挖掉身体**：
- 实测日志：`擦除=439`（比纯剑刃 201 多出 238）→ 剑柄带把持剑手臂/躯干成片擦成透明 → **"身躯一半消失"**。**结论：帧内剑柄与身体（持剑的手）在像素级重叠，帧级擦除永远无法两全**。
- **已回退**：`HiltBandPx=-1`（剑柄带禁用，三处守卫 `HiltBandPx>0`）→ 身体恢复、只保留安全红暗剑刃擦除。
- **剑柄残留的正解 = 部件贴图（sprite2）方案**：最终渲染 = 帧 RG(UV) 采样 `PartTex_Sword`。`SetSprite2` 把部件 rect 原点编码进顶点色（`G=96 = rect.x/2=192/2`）。下一步应从 `_PartTex` 克隆里抹掉剑区（亮银像素），而非改帧。离线 UV 解码 4 组合均无法稳定区分剑/身体，需运行时探针或读取真实 shader。
- **⚔️ 静默挥剑音效**：`Swordsman.Attack()` 会播 `swingSound("Sfx/English/Sword/Swing")`（"剑劈砍特效"的听觉部分）→ `ApplyToAgent` 改为 `"Sfx/English/Spear"`。
- **残留**：① 剑柄在少数非红帧仍可见（待 sprite2 方案）；② 身体仍播基底挥剑动画（手臂大幅横挥）——要彻底去掉需压制/替换 "Attack" 动画（涉及攻击状态机，风险高，未做）。

**🔬 五次进展（2026-08-15，日志复核 + PartTex 探针出结果 → sprite2 亮银擦除已实现）**：
- **日志复核（跨 5 次启动，`BadNorthBlackSpearman1.3.log` 9470 行）**：
  - `[近战] GetAttack` 共 16 次全部 `dmg=1.6 kb=2.5 stun=1.2 sound=Spear` → 普通攻击确实走长矛 Patch（不是剑）。
  - `[近战] 攻击开始/突刺中/攻击结束` 序列完整（10 次攻击）；`攻击开始 dist=1.30` 是 Swordsman 跳扑触发阈值 `radius+radius+range=0.95m`（2D navPos）+ 3D transform 虚高，非剑逻辑。
  - `range=0.69` 实测成立（16:39 起新 DLL；16:13 的 0.11 是旧 DLL `radius×0.7×1.3`，同一日志文件累计了 5 次启动）。
  - 突刺帧 `spearWorldRot` 大多为纯 yaw（0, X, 0）→ 鬼畜已消；少数带 x/z 分量出现在 agent 被击退自旋时，非矛抖动。
- **PartTex 探针结论（4 组合 UV 解码采样 `PartTex_Median_BlurAlpha`）**：剑帧色（红暗 bbox 中心）→ Part=**亮银不透明**（159~189,144~186,137~189）；身体帧色 → Part=**暗 (33,26,24)**。剑区在部件贴图里是"金属亮银"，与暗色身体可分离 → 剑柄残留"克隆 PartTex 抹亮银"方案成立。
- **已实现（SwordRemover，0 警告 0 错误）**：`EraseSilverPixels`（亮银=中性灰 r>110 g>100 b>90 且 |r-b|、|g-b|<60，a>128）+ `GetErasedSprite2` 改用亮银擦除 + `Sprite2SafetyRatio=35%`（PartTex 以剑为主体，沿用 20% 会拒擦）+ 擦除区 bbox 日志；探针新增"剑柄像素"采样（剑刃中线向身体侧找非红像素），下一轮日志直接回答"剑柄在 PartTex 是不是亮银"。
- **部署**：Debug 构建 77824B 已覆盖到 plugins（游戏已关）。
- **待验证（下一轮实测）**：① 日志里 `[去剑] sprite2 已去剑(亮银) ... bbox=...` 是否出现（还是 0 命中被拒）；② 游戏内剑柄是否消失、身体是否完好。

**🔬 六次进展（2026-08-15，实测结论：剑柄无解 → 换"长矛穿刺"根治）**：
- **sprite2 亮银擦除实测被安全阀拒绝**：`亮银命中 2681/6571 = 40.8%` > 35% → 跳过（sprite2 未替换）。PartTex_Sword 单元里亮银像素占近半（剑刃+大块装饰），按颜色擦会把单元挖空。
- **剑柄在 PartTex 里是暗色、与身体同色**（探针新增剑柄采样）：剑柄帧色(97,96) 四种解码全部命中 `(33,26,24)/(66,48,41)` —— 与身体(33,26,24)无法按颜色区分。**帧级/颜色级/部件级三路全部证明：剑柄视觉残留无法靠像素擦除解决。**
- **转方向：不擦剑柄，改为"长矛穿刺"攻击（抑制挥剑动画）**——用户反馈"用长矛执行剑的劈砍"（攻击时身体播 Onehanded 挥剑帧+跳扑）。已实现：
  - `Plugin.SwordsmanAttackPrefix`（Patch `Swordsman.Attack`）：黑矛兵攻击**不播挥剑动画**、去掉 hop 前置 → 站桩刺击；保留 swingSound(长矛音效)/MaybeParry。
  - `Plugin.SwordsmanAttackUpdatePrefix`（Patch `Swordsman.AttackUpdate`）：攻击时长由 `SpearChargeComponent.MeleeAttackDuration=0.5s` 控制（原版靠挥剑动画播完 animationDone）。
  - `SpearChargeComponent.UpdateMeleeThrust`：矛刺到位瞬间手动 `Swordsman.FirstHit()`（原版由挥剑动画事件触发伤害，不播动画必须自己触发）→ 走 GetAttack(长矛伤害)。
  - 副作用：攻击不再播挥剑动画 → **攻击时剑柄不再出现**；待机/移动帧剑柄是否残留待 `[近战·待机帧]` 新日志确认。
- **部署**：Debug 81408B 已覆盖到 plugins。
- **待验证（下一轮实测）**：① 攻击是否"站桩端矛戳刺"、攻速 0.5s/次是否合理；② `[近战·待机帧]` 日志显示待机用哪帧（若仍 Onehanded → 待机剑柄仍在，需盾牌遮挡或接受）；③ GetAttack 仍正常触发（伤害）。


---

### 🟡 P2-8：刺击"小的抽动" + 冲锋冷却 10s + 盾牌格挡（2026-08-15，✅ 定稿）

**现象**：近战刺击几乎正常，但矛仍有小幅抖动/抽动；冲锋冷却 4.25s 偏短，用户指定延长到 10s。

**刺击抽动根因（两处）**：
1. **突刺位移每帧重算**：旧版 `dirLocal = InverseTransformDirection(_thrustDirWorld)` 每帧执行；攻击期间 `Plugin.SwordsmanAttackUpdatePrefix` 每帧 `SetDirection(活动目标当前位置)`（SetDirection 是**瞬时** snap，Agent.cs:760），身体朝向随目标位移/自旋逐帧阶跃 → 本地偏移逐帧跳变 → 矛位置微小抖动。
2. **刺击朝向 roll 翻转**：旧式 `LookRotation(dir, agent.right)` 在目标位于角色侧向（dir ∥ agent.right）时退化，roll 翻转 180°（矛精灵上下颠倒；日志 `spearWorldRot=(0,179.5,180)`）。

**修复（攻击开始瞬间两处同帧锁死 + 全程零重算）**：
- `SetDirection(_thrustDirWorld)` 先把身体 snap 对准突刺方向；随后 `_thrustOffsetLocal = InverseTransformDirection(dir) × ThrustDistance` **只算一次**，攻击中仅 `localPosition = _spearBaseLocalPos + _thrustOffsetLocal × _thrust`。
- 刺击朝向改 `LookRotation(dir, cross(up, dir))`（虚拟 right = cross(worldUp, dir) 恒 ⊥ dir，永不翻转；agent 正对目标时 ≡ 旧式，观感零差异；⚠️ 首版误写 `cross(dir, up)` 符号反了 = −agent.right → 验证日志 `spearWorldRot=(0,X,180)` 与冲锋举矛 `(0,X,0)` 不一致，已修正）。
- `Plugin.SwordsmanAttackUpdatePrefix` 攻击期间面向改用 `SpearChargeComponent.LockedThrustDirection`（固定值），不再追活动目标。

**冲锋冷却**：`CooldownTime` 4.25s → **10s**（`SpearChargeComponent.cs`），不改冲锋表现本身。

**盾牌格挡**（`BlackSpearmanShield.cs`，复刻 `Shield.ModifyAttack`）：近战正面归零 / 箭矢×0.05 弹开·砸落 / 飞斧归零 / 长矛×0.2；`EnableShield` cfg=false 时仅视觉。

**待验证（下一轮实测）**：① 游戏内刺击是否完全稳定（不再抽动）；② 盾牌正面判定（`facing<=0.5f`）与视觉朝向是否一致；③ 冲锋 10s 冷却节奏。

**✅ 实测验证（2026-08-15 日志复核 + 修正 roll 符号）**：
- **刺击本地偏移已完全锁死**：全段突刺 `spearLocalPos=(0.000, 0.168, z)`，x/y 恒 0.000/0.168，z 严格按 thrust 推进（0.072 → 0.522 = 基座 0.072 + 0.45×thrust），无逐帧跳变 → "小的抽动"消除。
- **y 翻转（矛反指）已消除**：突刺帧 `spearWorldRot=(0.0, y, 180.0)`，y 与锁定方向一致（如 89.9/111.0/91.5/117.3/86.7…），不再出现旧退化日志 `(0,179.5,180)`。
- **⚠️ 发现 roll 符号错误并修正**：实测突刺 `spearWorldRot` 全为 `(0,X,180)`，而冲锋/原版举矛是 `(0,X,0)` —— 同一条矛两套姿态 roll 差 180°。根因：修复代码 `Cross(dir, up)` 符号反了（= −agent.right，正对目标时并非文档声称的 ≡ agent.right）。已改为 `Cross(up, dir)`，正对目标时 ≡ 旧式/冲锋举矛观感。
- **盾牌近战格挡生效**：`[盾牌] 格挡近战 ... facing=0.85 / 0.97` 两条日志出现，正面近战伤害归零走 `Shield` 音效。`挡箭/挡飞斧/格挡长矛` 未在本轮交战触发（测试波次玩家方只有 Standard/SwordShield，无弓手/飞斧/长矛），逻辑路径在代码里已就位、待有对应兵种的波次复测。
- **冲锋冷却 10s**：`CooldownTime = 10f` 常量确认；冲锋批次间夹长段近战/待机（不再是 4.25s 连冲）。逐 Agent 的"二次冲锋间隔"在本日志无法从交错输出中单独摘出，但状态机 `EndCharge → _phaseTimer=10f` 是确定性保证。
- **仍遗留（本轮日志复现）**：① `[动画·溯源]` Clash 告警仍出现（黑矛兵被打时 `Swordsman.ClashActivate` 播放 Onehanded Clash 帧）——可选方案：`Agent.PlayAnimation` 补丁把黑矛兵 Clash 帧替换为 Idle；② `[去剑] sprite2 亮银命中 2681/6571 疑似误擦 → 跳过`（剑柄残留，已知无解 → 盾牌遮挡）。

**🔧 八次进展（2026-08-15，盾牌美术根治：改剑盾兵基底 + 删死代码 + 注释精简）**：
- **盾牌美术不出现的根因**：旧方案在 `Viking_Sword` 基底上运行时克隆"我方剑盾兵盾牌"做静态遮挡（`MountShieldCover`），克隆体带 `LevelMesh/BodyColoredMesh` 但不进渲染管线 → 看不到盾牌。
- **按用户建议改用 `Viking_SwordShield` 基底并保留盾牌**：剥离模板只销毁逻辑组件（Arsonist/Shield 组件）、**不再禁用盾牌子对象**，只按名称禁用剑/武器/瞄准骨（`VisualChildNameKeys` 去掉 shield/盾）；盾牌随基底正常渲染，擦除剑刃的 `SwordRemover` 方案不变。`BlackSpearmanShield` 直接挂在基底盾牌子对象上（`FindShieldTransform`），删除 `MountShieldCover/FindShieldTemplate`。
- **删除死代码**：① `SpearStabAction.cs` + `EnableStab` cfg（全日志 `[Stab]` 0 次——近战刺击早已由 `Swordsman.Attack`→`UpdateMeleeThrust` 接管，`MaybeAct` 在 ready/hunting 不再被调度，永不触发）；② `SpearChargeComponent.CaptureScreenshotOnce`（该 Unity 2018.4 构建 `Application.CaptureScreenshot` 每次必抛异常，截图取证从未成功）；③ 启动日志 `Stab=` 字段。
- **注释精简**：清理 Plugin.cs / SpearChargeComponent.cs / SwordRemover.cs / BlackSpearmanShield.cs / SpearVisual.cs / BSLog.cs 的冗长注释（保留全部 ⚠️ Mono/mscorlib 雷区、Func 类型加载、ReferenceEquals 判空等关键警告）。
- **待实测**：① 黑矛兵身上是否出现可见的剑盾兵盾牌（日志应见 `[WEAPON] 使用基底盾牌: Shield ...`）；② 盾牌正面判定（`facing<=0.5f`）与视觉朝向是否一致；③ 剑刃擦除质量是否与 `Viking_Sword` 基底一致（基底动画帧同是 Onehanded 系列，预期一致）。

**🔧 九次进展（2026-08-15，盾牌日志两维化 + 预制件装配大改）**：
- **背景**：此前"判断盾牌存在"只验证了逻辑层（子对象找到/格挡触发），**忽略了美术效果是否真的呈现**（mesh/材质/渲染管线/是否上屏）。用户要求日志与预制件必须同时反映两个问题。
- **日志两维化约定（此后固定）**：
  - `[盾牌·美术资源]`＝静态检查：子对象是否存在、`Renderer/Mesh/Sprite/材质`是否齐备有效、`LevelMesh/BodyColoredMesh`渲染管线组件、外观判定（有任何有效渲染资源才"应有可见外观"）。
  - `[盾牌·实际效果]`＝运行期动态：`isVisible`是否真的上屏、本地/世界姿态、距身、朝外Dot、格挡触发计数（`块[近战/箭/斧/矛]`）。
  - 判定盾牌存在 = 两个维度都通过；任一维度 FAIL 都要按对应维度排查（美术资源→看 mesh/材质/管线组件；实际效果→看 isVisible/姿态/计数）。
- **预制件装配大改（BlackSpearmanWeapon.MountShield）**：① 基底盾牌（名称关键字 shield/盾）→ ② 兜底克隆场上剑盾兵盾牌 → 都没有才告警；挂载后先跑"美术资源"完整体检（`DumpShieldHealth`），再跑姿态退化自愈（scale≈0/陷入身体 → 重置到身左侧默认姿态）；`BlackSpearmanShield` 负责"实际效果"体检（每 30s，仅状态变化或不可见时输出，不可见即 WARN）。
- **剥离层配合（Plugin.BuildStrippedTemplate）**：销毁 `Shield` 组件前先用权威引用 `Shield.shield`（兜底名称关键字）记录盾牌 → 剥离日志输出 `盾牌美术[美术资源]=Shield active=True`，避免"组件销毁后再也定位不到盾牌"。
- **F8 扩展**：新增 `DumpBlackSpearmanShields()`，对每个黑矛兵全量重跑两维体检。
- **待实测**：① 挂载日志应见 `[盾牌·美术资源] 使用基底盾牌子对象: Shield` + 完整体检（`MeshRenderer`/`LevelMesh`/`BodyColoredMesh` 应全 ✓）；② 15s 后出现 `[盾牌·实际效果] 可见=是 ... 块[...]`；③ 若"外观判定 不通过"→ 盾牌必然不可见，直接往美术资源维度查。


**🔧 十次进展（2026-08-15，新基底剑刃根因 + 移盾遮蔽剑柄）**：
- **实测结论**：盾牌两维全通过（`可见=是` + `格挡近战 facing=0.88/1.00/0.60/0.72/0.99` 多起触发）；但**剑刃仍可见 + 白框闪烁**。
- **新基底剑刃根因（日志 + 运行时探针定位）**：换 `Viking_SwordShield` 基底后，帧名从 `Onehanded*` 变成 `Swordsman*`，`SwordRemover` 探测失败自毁（日志 `[去剑] 未找到 Onehanded 帧...组件停用`，实例组件列表里确实没了 `SwordRemover`）→ 剑全程可见。剑不在身体帧里，而在 **sprite2 部件贴图 `PartTex_SwordShield`**（剑+盾 2D 部件，旧基底探针已证剑=亮银像素 (173,163,148)，由身体帧顶点色 R/G 编码 UV 采样）；攻击时随 Clash 帧挥扫 = **白框闪烁**。
- **源头去剑（SwordRemover 适配双基底）**：① 帧识别放宽为 `Onehanded*`/`Swordsman*`（`IsSwordFrameSprite`）；② sprite2 若为 `PartTex_SwordShield` 则**整块清空**（`GetBlankSprite2`：黑矛兵持矛+3D盾，2D 剑盾部件全不要；空单元精灵缓存共享、烘焙重置 sprite2 时可重入）；旧基底 `PartTex_Sword` 仍走亮银擦除。
- **移盾遮蔽剑柄（用户方案）**：`MountShield` 新增第⑥步——把盾牌贴到基底 `Weapon` 锚点（原本持剑处），保持朝前（`Dot(shield.forward,-攻向)>0.5` 格挡判定不受影响）；`BlackSpearmanShield` 新增 `swordAnchor` + `LateUpdate` 每帧跟随身体动画，覆盖 Animator 的盾位驱动。移盾后前 2 只输出完整两维体检。
- **白框排查增强**：`DumpShieldHealth` 修复 `LevelMesh/BodyColoredMesh` 判定（改 `GetComponentInChildren`——它们挂在 `ShieldAnim/Shield` 孙级，原逻辑误报 False）；新增**网格顶点色[0]** 输出（Palette 材质按顶点色采样色板，缺失/纯白 → 白框来源）。
- **待实测**：① 日志应见 `[去剑] sprite2 整块清空(剑盾基底) PartTex_SwordShield 清空=xxxpx` + `[盾牌·美术资源] 盾牌移到持剑位遮蔽剑柄: 锚点=Weapon ...`；② 游戏中剑刃/白框应消失（若白框仍在→查网格顶点色日志）；③ 移盾后 盾牌朝向/大小是否合理（偏移常量在 `RepositionShieldToSwordHand`，`a.radius*0.25/0.1`，可调）。




## 2. 已解决（历史记录）

| # | 问题 | 解决方案 | 状态 |
|---|---|---|---|
| 1 | 美术复用逻辑残留（Arsonist 抢占/双 Container/孤儿克隆） | 预制体剥离 `DestroyImmediate` + 移除手动 vikingClone | ✅ |
| 2 | 冲锋无位移（DoCharging 没推进 navPos） | 加 navPos 推进 + 硬同步 | ✅ |
| 3 | 冲锋触发窗口太窄（MaybeAct 只在 Idle） | Update 独立检测 | ✅ |
| 4 | 位移橡皮筋（maxSpeed=0） | movability=0.2，不设 maxSpeed=0 | ✅ |
| 5 | 跑步动画缺失（transform 硬同步滑行） | 删硬同步，让 Body.stepping 驱动 | ✅ |
| 6 | 矛没对准目标 | GetAttack/冲锋时矛前指 | ✅ |
| 7 | 命中判定太宽提前倒地 | 0.75→0.3→0.4m + navPos 精确位置 | ✅ |
| 8 | DoRetreat NavPos 崩溃 | navPos.valid 保护 | ✅ |
| 9 | 后退距离过远 | 1.2→0.6m | ✅ |
| 10 | 退到船建模上无法被攻击 | 后退全程 onMain 检查 + MoveTo 失败收尾 | ✅ |
| 11 | 身体透明（HideSwordVisual） | 放弃改 sprite2（身体贴图） | ✅ 关闭 |
| 12 | Twohanded 基底自带技能/难处理 | 回退 Viking_Sword | ✅ |
| 13 | 冲锋免伤/协同防重（Twohanded 借鉴） | attack.ignore + 锁定集合 | ✅ 保留 |
| 14 | 触发与大脑不同步（走路到阵前不冲锋） | 大脑目标优先 + 6m 扫描兜底 | ✅ |
| 15 | 命中判定领先可见矛尖 1m（隔空命中） | 命中锚点=视觉长矛 + AgentEnumerators | ✅ |
| 16 | 抵达爆发失效（沿途全命中后爆发=0） | 爆发对终点周围所有存活单位结算 | ✅ |
| 17 | 一次冲锋范围波及过大 | 线宽 0.5m + 冷却 4.25s | ✅ 定稿 |
| 18 | 登岛前日志刷屏 | 触发拦截诊断降频 3s→12s | ✅ |
| 19 | 刺击"小的抽动"（本地偏移每帧重算 + roll 翻转） | 攻击开始瞬间锁 `_thrustOffsetLocal` + 虚拟 right 稳定朝向 + 身体锁向 | ✅ 定稿 |
| 20 | 冲锋冷却过短 | 4.25s → 10s（用户指定） | ✅ 定稿 |
| 21 | 盾牌只有视觉、无格挡 | `BlackSpearmanShield`（复刻 `Shield.ModifyAttack`）+ `EnableShield` cfg | ✅ 定稿 |

---

## 3. 状态与后续

**✅ 技能效果实现成功（2026-08-14 定稿）**：P0-1/P0-2/P1-3 全部解决，可躲高收益达成——
玩家需放弃优先占据的窄道/少接战面地形、转入开阔地拉扯，或承担被冲散的风险。

**✅ 近战刺击已定稿（2026-08-15）**：站桩长矛穿刺（对齐我方 Spear 的 TestHit / 主×1副×0.33 贯穿 / `PrefabManager.hitEffect` 命中特效）+ 抽动修复 + 盾牌格挡。
- 刺击抽动根因：突刺本地偏移每帧重算 + 侧向攻击时矛朝向 roll 翻转 180° → 攻击开始瞬间锁定偏移与朝向、身体锁向。
- 盾牌：复刻 `Shield.ModifyAttack`（近战/箭矢/飞斧/长矛四类格挡），`EnableShield` cfg 可关（false=仅视觉）。
- 冲锋冷却：4.25s → 10s。

**📋 已知遗留问题（2026-08-15）**：
1. 剑柄与持剑手像素重叠、PartTex 同色，像素擦除三路（帧/颜色/部件）无解 → 盾牌遮挡方案（静态挂载、不随动画摆动）。
2. 身体仍播放 Onehanded 基底动画帧（挥剑/走路基底），矛前刺主导观感；待机时手部姿态仍是持剑。
3. 盾牌正面判定 `facing<=0.5f` 若与视觉朝向不符，需放宽阈值或调盾牌 `localRotation/localPosition`。
4. 冲锋为单体冲刺（敌方无 `EnglishFormationAgent`），列阵留作后续可选。

**后续可选优化（不影响当前效果）**：
1. 矛的视觉升级：原版 `idealSpearTipDir` + 弹簧插值平滑举矛（当前为 Slerp 12/s）。
2. 命中特效/音效：`PrefabManager.hitEffect` + 冲锋专用 Fabric 音效。
3. 列阵冲锋：按 squad index 计算 slot offset（原版 `positionInFormation`）。
4. 数值再调：`HitRadius` / `StabKnockback` / `EnergyDecayPerHit` 均可按手感微调。
**🔧 十一次进展（2026-08-16，白框根因：整块清空部件贴图 = 把身体一起清掉）**：
- **实测结论（用户）**：盾出现、长矛近战逻辑出现；但黑矛兵身体变成白框（像素材缺少）。
- **根因（离线资产 + 反编译定位）**：身体渲染走 `Unlit/ColoredCharacter` 着色器——动画帧 R/G 通道是 **UV 编码**（采样部件贴图 `PartTex_SwordShield`），真正的身体/剑/盾美术全在 **sprite2 部件单元**里。上一轮 `GetBlankSprite2` 把整个单元 6204px 清空 → 身体（和剑盾）一起消失 → 身体区域渲染为空 → 背后亮地面透出 = "白框"。旧基底 `PartTex_Sword` 亮银擦除只擦剑、保留身体所以从没白框。
- **反编译关键事实**：`SpriteAnimator.SetSprite2` 把 `_PartTex` 设为新 sprite 纹理并重编码顶点色 R/G（rect 原点）；着色器 `Unlit/ColoredCharacter` 用帧 R/G + 顶点色 RG 采样 `_PartTex` 得身体外观。**结论：剑/身体不可分离地共存于部件单元 → 整块清空必然丢身体。**
- **修复（SwordRemover 模式化，cfg 可切）**：
  - `RemoveSwordSprite2Mode=0`（**默认**）：保留原部件贴图，**只靠帧擦除去剑**（Swordsman 帧红暗剑像素已擦，日志 `擦除=2xx`）→ 身体最完整、无白框；3D 盾移到持剑位遮蔽残余剑柄。
  - `=1`：整块清空（旧方案，白框，留作对照）。
  - `=2`：只擦"纯亮银剑身"（r,g,b>150 中性亮色，新函数 `GetBrightErasedSprite2` + bbox 接壤擦 + 45% 安全阀）→ 去剑+保留身体折中。
  - 模式0 附带 `LogPartCellStats`：输出部件单元 不透明/亮银/bbox，若剑仍可见可据日志切模式2。
- **构建**：0 警告 0 错误，Debug **100864B** 已部署到 plugins（SHA 验证通过）。
- **待实测**：① 白框是否消失、身体是否完整（黑矛兵应是暗色维京轮廓）；② 剑是否仍在（在则日志看 `[去剑] sprite2 模式0保留原部件` 的亮银占比，切模式2）；③ 移盾后盾牌位置/朝向是否合理（偏移常量 `RepositionShieldToSwordHand` 的 `a.radius*0.25/0.1` 可调）。

**🔧 十二次进展（2026-08-16，白框仍在 → UV 感知亮采样擦除根治 + 删持剑手选项）**：
- **实测结论（用户）**：模式0后**身体完整出现**（暗色维京轮廓 ✅），但**白框还在** + **持剑的手臂未被盾牌遮挡**。用户建议"直接删掉持剑的手（不知是否可行）"。
- **白框根因（本轮日志 + 离线分析定案）**：
  1. 帧纹理 R/G = UV 编码，渲染 = 帧像素按 UV 采样部件单元。**白框 = 解码 UV 采样到"亮银部件像素"的帧像素**（渲染成白/亮块）。
  2. 运行时 ETC2 压缩让 `PartTex_SwordShield` 局部变亮：亮像素 bbox 从**离线 y2~50** 膨胀到**运行时 y0~105**（含身体上部区域）。
  3. 旧红暗擦除（R>70,G<40,B<20）只抓"帧色红暗"的剑刃像素；**身体帧像素 G 高、不满足红暗，但采样到亮部件 → 白框永远擦不掉**。
  4. 离线佐证：`analyze_whitebox2.py` 统计 Swordsman0002 帧 1723 不透明像素 → 29 个亮采样像素**全部恰好是红暗**；运行时亮区更大、身体像素也会命中 → 需 UV 判定。
- **修复（`RemoveSwordFrameUVErase=true`，默认开）**：`EraseSwordInFrame`/`PreEraseAllOnehanded` 新增 **UV 感知亮采样擦除**——帧像素 `(R/255,G/255)` 解码到部件单元坐标，采样到 `r,g,b>150 & a>128` 部件像素就擦透明。暗身体像素（采样暗部件）不受影响。新函数 `EnsurePartCache`（部件克隆缓存，静态共享，按纹理实例 ID 重建）+ `BuildPartEraseMask` + `IsPartErase` + `CountEraseScan`。
- **删持剑手（`RemoveSwordFrameUVHalo`，默认 0）**：>0 时把"解码 UV 距亮部件像素 ≤N 部件像素"的帧像素也擦（光晕吃手/护手/剑刃边缘），真正实现"删掉持剑的手"。安全阀三指标：红暗>20% / 亮采样>45% / **光晕>15%** 任一超限跳过该帧。
- **顺带修复**：
  - `PreEraseAllOnehanded` 只擦 Onehanded 帧 → 新基底 Swordsman 帧**从未预擦**（首帧剑闪回仍在）→ 放宽为 Onehanded+Swordsman 并加入 UV 擦除；
  - 预擦被安全阀跳过的帧之前**错误标记 `_erasedRects`**（永不重试）→ 改为擦成功/无可擦才标记，跳过帧留给逐帧路径重试；
  - 删除死代码 `IsOnehandedSprite/CountMatch/CountRed`（被 `CountEraseScan` 取代）。
- **日志暴增（"暴露更多问题"）**：
  - 逐帧 `[去剑] 帧 X 去剑成功 擦除=N（红暗=.. 亮采样=.. 光晕=..）`；
  - 一次性 `[去剑] UV部件缓存就绪`（亮像素数/光晕）+ `UV亮采样图`（B=白框像素 S=红暗 .=身体）+ `[去剑] 亮采样分析`（白框像素计数）；
  - `网格子对象材质块`：每个 MeshRenderer 的 block._MainTex/_PartTex（**验证去剑克隆覆盖 _MIRROR_ON 变体**——若镜像块仍是原纹理，朝左时就露剑/白框）；
  - F8 渲染诊断补每网格块纹理。
- **构建**：0 警告 0 错误，Debug **107008B** 已部署到 plugins（SHA 验证通过）；cfg 预写 `RemoveSwordFrameUVErase=true / RemoveSwordFrameUVHalo=0`。
- **待实测**：① 日志看 `UV部件缓存就绪 亮=N`（N 应为运行时部件亮像素数，几百量级）+ 首帧 `亮采样分析 亮采样(白框源)=M`（M>0 说明确实擦到了白框像素）；② 游戏内白框是否消失、身体是否仍完整；③ 若手/剑柄仍可见 → `RemoveSwordFrameUVHalo` 1→2→3 逐步加大（日志 `光晕=` 会报每次擦多少）；④ 日志看 `预擦除 Onehanded/Swordsman 帧 N 张` 是否出现（Swordsman 帧预擦生效=闪回修复）；⑤ 若白框反而出现在别处 → 看 `网格子对象材质块` 是否有块 `_MainTex` 不是去剑克隆。

**🔧 十三次进展（2026-08-16，实测推翻 UV 理论 → 亮剑部件擦除 + 材质块修复）**：
- **实测（用户）**：剑刃剑柄存在、盾牌未盖住剑柄的手（疑似视角）、白框存在。用户指示**先去掉白框**。
- **数据推翻第十二轮 UV 理论**：
  1. 全帧 `[去剑] 帧 X 去剑成功 亮采样=0`——新 UV 亮采样擦除在游戏内 **0 命中**（`亮采样分析: 亮采样(白框源)=0`）。运行时 ETC2 的亮像素位置与离线 PNG 完全不同（离线 29 个白框像素运行时不存在）→ **"白框=帧像素采样到亮部件"在运行时不成立**。
  2. F8 转储：**身体 SpriteAnimator 下 4 个 MeshRenderer 前 2 个 `block._MainTex/_PartTex` 全 null**（`Unlit/ColoredCharacter` 默认白色 → 若网格有几何即白框源）；后 2 个有 `_MainTex`（名字=图集，克隆/原始无法区分）但 **`_PartTex=null`**（游戏每帧用原图集重写 block 时把 _PartTex 清掉）。Shadow 也是同样"前 null 后有"结构 → 空块渲染器可能是闲置模板，需顶点数/实例 ID 确诊。
  3. 离线渲染模拟（`tmpfix/render_debug.py`，cand0=游戏内解码）：模式0下**部件贴图亮银剑刃经帧 UV 采样渲染成亮/白色 = 白框+亮剑直接来源**；**模式2（擦亮银部件像素）后暗身体保留（706/798px）、剑区 93~141px 变洞（预期挖剑）**。
- **修复**：
  - cfg 默认 `RemoveSwordSprite2Mode` 0→**2**：部件贴图只擦亮银亮色像素（剑刃+2D盾从 `PartTex_SwordShield` 抹掉、暗色身体保留）；`GetBrightErasedSprite2` 加 **35% 亮银占比安全阀**（`bright > opaque*0.35` → 退化模式0，防 ETC2 把身体也染亮被误擦）。
  - **`RepairBodyMaterialBlocks`**（每帧）：把去剑克隆+部件贴图强制写入全部 `ColoredCharacter` MeshRenderer 材质块——空块渲染器补纹理（白框源）、游戏每帧用原图集覆盖 block 必须每帧重写；`GetPropertyBlock` 先拷全量再覆盖 `_MainTex/_PartTex`（`_BloodTex`/`_Mirror` 等保留）。
  - `_sa.block` 更新时**保留 `_PartTex`**（SetTexture 后再 ComittBlock，不再清掉部件贴图）。
  - 顺带修诊断 bug：`IsPartBrightExact` 原在像素清零后调用（UV 解码变 cell(0,0)），把真亮采样误标成"光晕"（上一轮 `UVHalo=0 仍报光晕=6` 的来源）→ 先判纯亮再清零。实际模式0+UVErase 每帧约擦 6px 亮采样，远不足以消白框——白框主体是部件贴图亮剑，须模式2。
  - 诊断：一次性 `身体网格详细`（顶点数/isVisible/`_MainTex` 实例 ID=克隆或原始图集）；F8 渲染诊断补顶点数 + `←去剑克隆✓`（实例 ID，`SwordRemover.IsSharedClone`）。
- **构建/部署**：0 警告 0 错误，Debug **110080B**，SHA `C14ED2F4…`；cfg 已预写 `RemoveSwordSprite2Mode=2`。
- **待实测**：① 白框是否消失（若仍在不复现 → 看 `身体网格详细` 空块渲染器的 verts：>0 则白框来自空几何渲染器且修复应已补纹理）；② 剑刃/剑柄是否消失（2D 剑盾从部件贴图抹掉）；③ 身体是否仍完整暗色轮廓（剑区挖洞=预期，非身体透明）；④ 日志 `[去剑] sprite2 亮银剑身擦除(剑盾基底)` 的 bbox 是否只罩剑区；⑤ 日志 `身体材质块修复: 补纹理渲染器 N 个`；⑥ 盾牌位置若仍不遮手 → 调 `RepositionShieldToSwordHand`（`a.radius*0.25/0.1`）。

**🔧 十四次进展（2026-08-16，白框根治✅ 后实测：剩剑柄 + 持矛手脱离身躯 → flood 擦剑柄 + 矛挂手位）**：
- **实测（用户）**：白框已消失 ✅；剩余两问题——① 剑柄依旧可见；② 黑矛兵持长矛的手脱离身躯，导致攻击范围异常大、观感差。
- **问题①根因（剑柄残留，日志三证据）**：
  1. `[去剑] sprite2 亮银剑身擦除(剑盾基底) PartTex_SwordShield 擦除=833/6204 bbox=(133,0)-(191,105) 擦bbox=False`——模式2只擦"纯亮银"(r,g,b>150) 833px（剑刃+2D盾已抹），但 **bbox 接壤擦被安全阀拒绝**：剑刃与 2D 盾亮像素合流，bboxArea=59×106=6254 > 不透明×45%=2792 → `擦bbox=False` → 剑柄/护手区从未被擦；
  2. `[去剑·PartTex探针] cand0 剑帧色=(102,14)→Part=(170,146,115,255) 剑柄→Part=(54,50,49,255)` 及第二帧 `剑柄→Part=(173,158,148,26)`——**剑柄在运行时 ETC2 部件贴图是暗灰/半透明、不是亮银** → 纯亮阈值永远漏（`擦bbox` 又拒了接壤擦 → 两条路都断）；
  3. 帧级 UVHalo 的部件掩码只标记"亮>150"像素 → 暗灰剑柄不在掩码内 → 帧擦也够不着。
  → 结论：剑柄只能在部件贴图层用"从亮银区向外 flood"吃掉（身体暗色≈(33,26,24) 是天然屏障不被吃），或靠盾牌遮挡。
- **问题②根因（持矛手脱离身躯）**：长矛 `MountSpear` 用固定偏移 `(0, radius×1.4, radius×0.6)`——矛根在**身体正中**；基底剑盾兵"持剑的手"在 **Weapon 锚点（距身 ~0.20m、偏离身体中心）** → 矛根与手错位 → "手没握着矛、矛悬在身前"；矛长 0.6m 再叠加错位 → 有效攻击范围显得异常大。
- **修复（日志 + 预制件）**：
  - **`RemoveSwordSprite2GripBand`（新 cfg，默认 2）**：`GetBrightErasedSprite2` 擦完亮银时记录擦除亮银像素坐标 → 新函数 `GripFloodErase` 从它们出发 **BFS 扩散 GripFloodPx 像素**，吃掉"不透明且非身体暗色(r<45&&g<38&&b<33)"的相连像素（剑柄/护手/持剑手）；身体暗色是天然屏障；提交前校验 flood≤300px 且 bbox≤1200 单元，超限放弃防误擦。日志：`剑柄flood=N（采样首/中=(r,g,b,a)）`。
  - **`SpearMountToHand`（新 cfg，默认 true）**：`MountSpear` 矛根改挂 `FindSwordAnchor`（Weapon 锚点=持剑手位），`localPos=InverseTransformPoint(anchor.position)+(0, radius*0.1, radius*0.15)`（矛尖朝前；找不到锚点退回旧偏移）。日志 `[WEAPON] 长矛握持位: 锚点=… 锚点localPos=… → 矛根localPos=…`。
  - **盾牌前移 0.25→0.12**（`RepositionShieldToSwordHand`）：盾面贴住持剑手真正盖剑柄；新增 `覆盖持剑手=是✓/否✗` 日志（Weapon 锚点是否落在盾 Renderer bounds 内）+ `锚点距盾心=…m`。
  - **诊断**：新函数 `DumpGripResidue`——擦完后原亮银 bbox 内剩余像素分类统计（暗灰剑柄/亮灰护手/暖色皮肤/亮银残）+ `剑柄残留·定位图`（g=暗灰 G=亮灰 s=皮肤 b=身体，取暗灰最多一行±8 行定位剑柄）；`Diagnostics.DumpBlackSpearmanRender` 新增 `持矛手对齐: 矛根…|锚点…|矛根↔手距离=…`（0=矛根正好在手上）。
- **构建/部署**：0 警告 0 错误，Debug **116224B**，SHA `DCA42BCEE2F8E969390423F5A7DF4CC9B5A5973A`；cfg 已预写 `RemoveSwordSprite2GripBand=2 / SpearMountToHand=true`。
- **待实测**：
  ① 剑柄是否消失——日志看 `剑柄flood=N`（N>0 = flood 生效）+ `剑柄残留诊断 暗灰剑柄=N`（应≈0）+ `剑柄残留·定位图`；若仍在 → `RemoveSwordSprite2GripBand` 3→4 加大，或看盾牌 `覆盖持剑手=是/否`（否 → 继续调 `RepositionShieldToSwordHand`）；
  ② 身体是否被误擦（躯干出现洞）→ `RemoveSwordSprite2GripBand=0` 关闭，改盾牌遮挡；
  ③ 持矛手对齐——日志 `[WEAPON] 长矛握持位` 的矛根 localPos 应与锚点 localPos 接近；F8 `矛根↔手距离` ≈0；游戏内手应握着矛、矛不再悬空；
  ④ 攻击范围观感——矛挂手位后，有效攻击范围应从"身体中心+矛长"恢复为"手位+矛长"。<｜end▁of▁thinking｜>

**🔧 十五次进展（2026-08-16，问题未解决 → flood 换"剑柄改色" + 矛跟手 + 盾贴手）**
- **实测**：用户反馈"问题未能解决"。日志铁证：`剑柄flood=0`——flood 从头到尾**一个像素都没吃到**（`暗灰剑柄=1893` 依旧在）；`覆盖持剑手=否✗`（盾牌依旧没盖住手）。
- **flood 为何 0 命中（离线分析定案）**：
  1. `analyze_uv.py`（离线 UV 解码渲染模拟）：剑是**竖握**——剑刃(亮银)在上（帧 rows2-14），**剑柄(暗灰 54,50,49)横在胸口**（帧 rows17-26 采样部件区 rows47-88）；
  2. `analyze_grip_frames.py`（全 Swordsman 帧）：每帧 ~150-200 像素采样暗灰剑柄区，**帧 bbox 全部集中在角色上半身剑柄带**（非全身散落）→ 剑柄是画在身体上的带子；
  3. 剑柄带与亮银剑刃之间隔着**身体暗色带** → BFS 从亮银种子出发第一步就被屏障拦下 → `flood=0` 不是安全阀误判，是**连通性根本不成立**；
  4. 且"擦除(透明)"会把胸口剑柄带**挖成洞**（背后地面透出）或白框——剑柄不能擦、只能改色。
- **修复①（剑柄改身体色，替代 flood）**：
  - 离线渲染模拟 `validate_grip_recolor.py`（用运行时 ETC2 贴图，渲染=帧 R/G 编码 UV 采样部件贴图×黑染色）定案：**把单元内暗灰(40≤r≤100,|r-b|≤25)+亮灰护手(100<r<150 中性)改为身体暗色(33,26,24)、保留 alpha** → 帧渲染 `剑柄色像素 1402→0`、身体轮廓完好（1536 身体色，洞=187 剑刃洞不变），无新增洞/白框；
  - `GetBrightErasedSprite2` 新增 `RecolorGripToBody` 改色一遍（删除 `GripFloodErase`）；日志 `剑柄flood=` → `剑柄改色=Npx`；`RemoveSwordSprite2GripBand` cfg 语义改为">0 启用改色"，默认仍 2；
  - 残留诊断 `DumpGripResidue` 预期：`暗灰剑柄≈0`（改色后归入身体暗色类）、`亮银残=0`、皮肤(手)保留。
- **修复②（矛根每帧跟手）**：`SpearChargeComponent` 新增 `TrackSpearToHand()`——Setup 记录"矛根相对 Weapon 锚点的偏移"；每帧 `_spearBaseLocalPos = 当前手位 + 偏移`，突刺/冲锋/待机时矛根始终贴在手上（旧版矛根固定于挂载瞬间，手随动画一动就脱开=观感"持矛手脱离身躯"）。`_handAnchor=null` 退回旧行为。
- **修复③（盾真正贴手）**：`RepositionShieldToSwordHand` 前移 0.12→**0.05**、抬高 0.1→**0.02**、放大 1.2→**1.5**（盾心落在 Weapon 锚点上）；`BlackSpearmanShield.LateUpdate` 偏移同步（旧 0.25/0.1 与挂载 0.12 不一致、每帧被 LateUpdate 覆盖成 0.25 才是"覆盖失败"的元凶）→ `覆盖持剑手=是✓` 应达成。
- **构建/部署**：0 警告 0 错误，Debug **115200B**，SHA `405C2D2D…`；已部署 plugins 并哈希验证 MATCH。
- **待实测**：① 剑柄带是否消失（胸口无暗灰横带）、身体完整无洞；② 日志 `剑柄改色=Npx`（几千量级）+ `剑柄残留诊断 暗灰剑柄≈0`；③ 跑步/刺击时矛根始终贴手（F8 `矛根↔手距离`≈0 且移动中不变）；④ `覆盖持剑手=是✓`；⑤ 攻击范围观感恢复。

**🔧 十六次进展（2026-08-16，核对 BadNorthDatabase 定案 → 路线A\"整剑改身体色\"零洞）**：
- **数据库核对结论（回答用户\"有没有天然剑体相分离的单位\"）**：
  1. **玩家方 Pikeman 天然武器体分离**：反编译 `Spear.cs`（数据库 09.01 §14.2-14.4）——矛=独立 `BatchedSprite spearSprite`（原生 `Spear_0/1/2.png`），身体 SpriteAnimator 无武器；游戏官方长矛兵即此架构（我们 `MountSpear` 已复刻方案A）。
  2. **帧单位无关**：敌人 `Swordsman0002` × 我方无剑单元 `PartTex_English`(0,0) 解码渲染自洽（头盔+圆盾+腿，无剑）；`× PartTex_Sword`(192,0) = 竖握剑（剑柄在手位）；`× PartTex_SwordShield`(128,0) = 横剑胸口（现基底）。差异全在\"部件贴图绘画\"层 → 逻辑零重写（敌我渲染管线相同）。
  3. **决策（用户选路线A）**：保留维京剑盾兵身体观感，把部件单元整把剑（亮银刃+暗灰柄+亮灰护手）**改身体色**而非换单元/擦透明。
- **离线验证 `validate_routeA_recolor.py`（运行时 ETC2 贴图）**：路线A 后 Swordsman0002/0001 → `剑柄/护手色=0`、`亮银残=0`、**洞=原始基准 158/205（零新增）**（原\"擦透明\"路径新增 29/65 洞）、身体色像素最多（1431/1258）。判据全过。
- **代码改动**：
  - `GetBrightErasedSprite2`（模式2）第二遍：亮银擦透明 → **改身体暗色(33,26,24)、保留 alpha**（bbox 内接壤像素同改）；日志 `擦除=` → `亮银改色=`（注明零洞）。
  - `Sprite2SafetyRatio` 0.35→**0.60**：改身体色无害（ETC2 增亮身体像素改回身体色=无害），仅留结构异常兜底。
  - `LateUpdate` 新增**路线A分支**：`_sa.sprite2==改色克隆` 时跳过帧擦透明（帧像素保持不透明、采样改色部件即身体色，消除帧级挖洞）；材质块每帧重写 `_PartTex=改色克隆`；烘焙重设 sprite2 自动退回帧擦除兜底。日志 `[去剑] 路线A生效…`。
  - Plugin.cfg 模式2描述更新为\"整剑改身体色、零洞无白框\"。
- **构建/部署**：0 警告 0 错误，Debug **115712B**，SHA `D00443D3…`；已部署 plugins 并哈希验证 MATCH。
- **待实测（重点）**：① 胸口剑柄带消失且**身体零洞**（对比十五轮的洞）；② 无白框；③ 维京观感保留；④ 矛根贴手+盾盖手。

**🔧 十七次进展（2026-08-16，用户实测反馈 → 回退"擦透明"、移除盾牌、修复抽动与举矛）**：
- **用户实测反馈**（对照十六轮 DLL）：
  1. **剑刃变成黑色、剑柄未变色**——路线A"改身体色"把亮银剑刃改成身体暗色后，剑刃轮廓在身体旁形成**黑色剑影**；剑柄带（暖色持剑手/未命中改色的暗灰柄）仍在胸口。用户指令：**"用之前的方法擦除掉剑刃，没想办法改变剑柄颜色"** → 回退"擦透明"、剑柄不处理。
  2. **删除盾牌效果和美术资源**——黑矛兵不需要盾。
  3. **存在抽动**——日志证据：冲锋穿透/被击退自旋时 `spearWorldRot=(340.4, 159.7, 109.5)`（矛 180° 翻转）；`PointSpearAtTarget` 朝身后目标 `LookRotation(dir, agent.right)` → roll 退化翻转 + 方向朝后。
  4. **船上/未判定敌人前长矛未树立**——`_hasSpearTarget` 初始 false、矛保持挂载时的水平姿态，直到第一次冲锋 `RaiseSpear` 才举矛 → 违背"长矛始终树立"设计。
  5. **问 `.bak_r15` 文件作用**。
- **改动①（回退"擦透明"，SwordRemover.cs）**：
  - `GetBrightErasedSprite2`（模式2）第二遍：`new Color32(33,26,24,a)` → **`new Color32(0,0,0,0)`**（亮银 + bbox 内接壤像素擦透明）；日志 `亮银改色=` → `擦除=`；`spr.name` `_NoSwordBodyColor` → `_NoSword`。
  - `Sprite2SafetyRatio` **0.60→0.35**：擦透明会把 ETC2 增亮身体像素挖成洞 → 收紧防误擦（改色方案的"改回身体色无害"前提不成立）。
  - `LateUpdate` **删除路线A跳过帧擦分支**：帧级 `EnsureErasedTexture`+`RepairBodyMaterialBlocks` 恢复每帧执行（与第十四轮一致）；删除 `_routeALogged`。
  - `RemoveSwordSprite2GripBand` 默认 **2→0**（`RecolorGripToBody` 不执行；功能保留，>0 可重开）。
  - 预期：剑区挖洞（离线验证：原擦透明路径新增 29/65 洞，可接受）、无黑色剑影、无白框；剑柄带保留（用户接受）。
- **改动②（移除盾牌，BlackSpearmanWeapon.cs + Plugin.cs）**：
  - `ShieldChildNameKeys = { "shield", "盾" }`；`EnableShield` 默认 **false**、语义=完全移除。
  - `BuildStrippedTemplate`（剥离模板）+ `RemoveSword`（运行时）在 `!EnableShield` 时禁用盾牌子对象；`MountShield` 直接 return（不挂 `BlackSpearmanShield`、不移盾遮蔽、不体检）。
  - `true` 可回退到"保留盾牌美术+格挡"（原第十五/十六轮行为）。
- **改动③（修抽动=矛翻转，SpearVisual.cs）**：
  - `TryGetAimRotation`：目标方向与 agent.forward 点积 <0.15 时 Slerp 混合回前半球（正后方→完全朝前），杜绝 180° 翻转；roll 改用虚拟 right=`cross(up, dir)`（恒 ⊥ dir、不随身体自旋退化），替代 `agent.transform.right`。冲锋/后退/刺击/GetAttack 全链路共用。
- **改动④（长矛始终树立，SpearVisual.cs + SpearChargeComponent.cs）**：
  - 新增 `TryGetRaisedRotation`：矛尖朝前上方（dir = fwd*0.6 + up*1.0，≈55° 抬举）。
  - 新增 `SpearChargeComponent.UpdateSpearPose()`：待机/移动/冷却时**无存活目标→举矛**、有目标→矛尖朝敌；`LateUpdate` 无目标分支 Slerp 抬回举矛姿态。船上/未判定敌人前长矛保持树立。
- **改动⑤（.bak_r15 说明）**：`BadNorthBlackSpearman1.3.dll.bak_r15`（116224B）= **第十六轮部署时对第十五轮 DLL 的手动备份**（第十五轮行为：剑柄改身体色、盾牌保留）。BepInEx 只加载 `.dll`，`.bak_r15` 不会被加载，**可随时删除**；本轮部署已生成 `.bak_r16`（115712B，第十六轮 DLL）同属回滚备份。
- **构建/部署**：0 警告 0 错误，Debug **116736B**，SHA `7D041156…`；已部署 plugins 并哈希验证 MATCH；cfg 已更新（`EnableShield=false`、`RemoveSwordSprite2GripBand=0`，描述同步）。
- **待实测**：① 剑刃擦除（无黑剑影、无白框，剑区有洞属预期）；② 盾牌完全消失；③ 抽动消除（冲锋中矛不再 180° 翻转）；④ 船上/无目标时长矛树立；⑤ 维京观感（角盔/皮甲/2D盾）保留。

---

## 第十八轮（2026-08-16）：剑柄改身体色 + 人物抽动双管修复与诊断 + .bak 说明确认

**用户指令**：
1. **剑柄需改色，和黑矛兵的身躯颜色一致**。
2. **黑矛兵人物存在抽动，找不到原因** → 请安排日志和预制件分析。
3. mod 文件夹中的 `.bak_r15`/`.bak_r16` 后缀文件。

### 1. 剑柄改身体色（恢复第十五轮改色功能）

- **前置**：第十七轮用户指定"不改剑柄颜色"，`RemoveSwordSprite2GripBand=0`。第十八轮用户改口：**剑柄颜色要与黑矛兵身躯颜色一致** → 重新启用 `RecolorGripToBody`。
- **改动**：`RemoveSwordSprite2GripBand` 默认 **0→2**（SwordRemover.cs `GripFloodPx=2` + Plugin.cs cfg 绑定 + 部署 cfg 同步）。
- **机制**（第十五轮已离线验证）：着色器以部件贴图为颜色源。把 PartTex_SwordShield 单元内 **暗灰剑柄 (40≤r≤100, |r-b|≤25) + 亮灰护手 (100<r<150 中性)** 改为 **身体暗色 (33,26,24)**、保留 alpha（不挖洞不白框）→ 胸口剑柄带渲染成与身躯一致的颜色。
- **预期日志**：`[去剑] sprite2 亮银剑身擦除… 剑柄改色≈1900px`；残留诊断 `暗灰剑柄≈0`、`亮灰护手/盾沿≈0`、`暖色皮肤(手)≈637` 保留、`亮银残=0`。
- **注意**：刃部仍按第十七轮"擦透明"（剑区挖洞可接受）；**不**恢复"改刃色"方案（那会造成黑色剑影，用户已否决）。

### 2. 人物抽动：根因定位 + 双管修复 + 诊断探针

**日志证据链（第十七轮 playtest）**：
- 刺击命中后：`[动画·溯源] ⚠️ 黑矛兵被要求播放 Clash 动画`，调用栈 `Agent.DealDamage → Swordsman.ModifyAttack → AgentState.SetActive → Swordsman.ClashActivate → Agent.PlayAnimation`；随后 `clip=Swordsman_Clash / body=slide:True` → **原版剑击滑动动画叠加在矛刺上 = 抽动①**。
- 冲锋中：`lag=0.89→1.18→1.31m`（`lag = |navPos.wPos − transform.position|`），且 `animSpeed=4.3~7.1`；冲锋结束恢复 `movability=1` 时角色被"弹回"到 navPos = **橡皮筋回弹 = 抽动②**。

**修复①（拦截原版 Clash 动画，Plugin.cs）**：
- 新增 Harmony 前缀 `SwordsmanClashActivatePrefix`：黑矛兵（`_done` 集合命中）直接 `return false` 跳过 `Swordsman.ClashActivate()`。
- 安全性：IL 反编译验证 `ClashActivate` 仅「播挥剑音效 + 设 animator Clash bool」（45 字节），伤害/击退在 `Agent.DealDamage` 内已结算，clash 状态生命周期照旧 → 拦截动画不影响伤害。
- 启动日志新增 `ClashBlock=OK`；若仍见 `[动画·溯源] Clash` 说明还有其它原版路径，交给 TwitchProbe 继续盯。

**修复②（冲锋橡皮筋，SpearChargeComponent.cs）**：
- `StartWindUp`：`movability 0.2/0.1 → 1/1`。原 0.2 抄自原版 `PikeChargeComponent.travelling`，但原版 navPos 是走路速度推进；本冲锋 navPos 以 5m/s 推进，movability=0.2 把 transform 追 navPos 限到 maxSpeed×0.2≈1m/s → 落后 1m+。拉满后身体紧贴 navPos。
- `EndCharge` 安全网：navPos 与 transform 差 >0.3m 时把 `navPos` 重新锚定到 `transform`（`new NavPos(navMesh, transform.position, true, 1f)`），防"弹跳回位"；日志 `[Charge] 收尾对齐`。

**诊断（TwitchProbe.cs 新增）**：
- `TwitchProbeComponent` 每个黑矛兵挂载（Plugin.ApplyToAgent）。逐帧监控 6 类异常并打 `[抽动]` 日志（含完整现场：pos/yaw/navLag/clip/norm/animSpeed/sprite/body/phase/矛 world+local 姿态）：
  1. 位置跳变 >0.35m/帧（非滑行）
  2. 朝向急转 >40°/帧（冲锋/后退除外）
  3. 动画倒退（同 clip 下 norm 回落 >0.3）
  4. 橡皮筋（navLag 差速变化 >0.35m/帧）
  5. 长矛本地 yaw 翻转 >90°/帧
  6. 精灵帧闪动（1s 内 ≥3 种帧名且变化 ≥4 次）
- 每实例 0.5s 节流防刷屏。下一次 playtest 若仍有抽动，`[抽动]` 日志的编号+现场直接给出根因。

**预制件分析（Diagnostics.cs F8 新增）**：
- `DumpPrefabAnalysis()`：VikingReference（type/bounty）+ 模板 `viking` + 运行实例 `agent` 的完整层级（Transform 树 + 每节点组件 + active + localPos/Rot）、Animator（controller 名 / animSpeed / updateMode / culling / 动画片段数 / 当前动画）、SpriteAnimator（sprite/sprite2 rect/color）、持剑锚点(Weapon 骨)路径、长矛姿态。
- 目的：确认黑矛兵 = SwordShield 基底（角盔/皮甲/2D盾）、去剑/去盾/挂矛后的层级是否还有残留动画源。

### 3. `.bak_r15` / `.bak_r16`（及新增 `.bak_r17`）说明

- `plugins/BadNorthBlackSpearman1.3.dll.bak_r15`（116224B）= 第十六轮部署时对第十五轮 DLL 的手动备份（行为：剑柄改身体色 + 盾牌保留）。
- `.bak_r16`（115712B）= 第十七轮部署时对第十六轮 DLL 的备份。
- `.bak_r17`（125440B）= 本轮部署时对第十七轮 DLL 的备份。
- **BepInEx 只加载 `.dll` 后缀**，`*.bak_rNN` 永不会被加载，纯回滚备份，**可随时删除**。

### 本轮改动文件

| 文件 | 改动 |
|---|---|
| SwordRemover.cs | `GripFloodPx` 0→2；注释/日志同步"第十八轮改身体色" |
| Plugin.cs | `RemoveSwordSprite2GripBand` 默认 2；新增 `ClashActivate` 拦截 Patch（`ClashBlock=OK`）；ApplyToAgent 挂 TwitchProbe |
| SpearChargeComponent.cs | 冲锋 movability→1/1；EndCharge navPos 重锚安全网；新增 `PhaseLabel` |
| TwitchProbe.cs | **新增**：`[抽动]` 逐帧诊断探针 |
| Diagnostics.cs | F8 新增 `DumpPrefabAnalysis()`（预制件分析） |
| 部署 cfg | `RemoveSwordSprite2GripBand=2` + 描述同步 |

**构建/部署**：0 警告 0 错误，Debug **125440B**，SHA `86CA16B2…`；已部署 plugins 并哈希验证 MATCH；`.bak_r17` 已生成。
**待实测**：① 剑柄带变身体色（无暗灰横带）；② 命中时不再播 Swordsman_Clash；③ 冲锋 navLag 保持小、结束不回弹；④ `[抽动]` 日志是否出现；⑤ 无目标/船上举矛。

---

## 第十九轮（2026-08-16）：第十八轮实测反馈 → "闪亮"根因 + 盾牌格挡 + 技能期可击杀

**用户实测反馈（第十八轮 playtest）**：
1. **剩余"抽动"看起来更像"美术素材的闪亮"**（不是身体橡皮筋——ClashBlock + movability 修复已生效，`[Charge] 收尾对齐` 正常收尾，冲锋结束不再弹回）。
2. **长矛突刺/冲锋打我方持盾单位时直接死亡**——缺"打到盾牌上的反馈和免伤"。
3. **希望黑矛兵在技能释放（举矛/冲锋/后退）过程中可被击杀**（平衡性）。

### 1. "闪亮"根因 = 去剑预擦除空转（SwordRemover.cs）—— 实锤

**第十八轮日志证据**：战斗/冲锋全程 `[去剑] 帧 SwordsmanXXXX 去剑成功 擦除=N` 逐帧、逐帧地懒擦（0002/0003/0004/0005/0011~0014/0016~0028 全是运行时擦的），说明**预擦除从未真正执行**——即"预擦除 N 张"日志为 0。

**根因（IL/代码级实锤）**：`EnsureErasedTexture` 里 `PreEraseAllOnehanded(tex)` 传的是 **共享克隆**（`GetSharedClone(srcTex)` 的产物），而函数内用 `ReferenceEquals(sprite.texture, tex)` 匹配帧——`sprite.texture` 是**源图集**，`ReferenceEquals(源, 克隆)` 恒 false → 帧列表恒空 → `frames.Count==0` 命中"无待擦帧也算完成" → `_preErased=true` 永久标记 → **所有帧退回运行时逐帧擦除**。

**为什么肉眼可见"闪亮"**：运行时逐帧擦 = 每帧"首次显示后才擦"（晚一帧），且 2048x1024 整图 `SetPixels32+Apply` 上传慢；冲锋 `animSpeed=4.7~7.7` 时新帧连续出现 → 剑刃在每个新帧上闪一帧才被擦 → 高强度剑刃闪回。

**修复**：
1. `PreEraseAllOnehanded` 入参改传**源纹理**（匹配 `sprite.texture`），像素读写改用**共享克隆**（与 `_MainTex` 同一份，擦完直接可见）。
2. `_preErased`（单 bool）→ `_preErasedTex`（按源纹理实例 ID 的 HashSet），多图集可各自独立预擦。
3. 预期日志变为 `[去剑] 预擦除 Onehanded/Swordsman 帧 N 张`（N>0），战斗/冲锋全程不再出现逐帧懒擦日志。

### 2. 长矛 vs 我方剑盾兵 = 正面格挡反馈 + 免伤（SpearChargeComponent.cs）

**现象**：黑矛兵突刺/冲锋/抵达爆发命中我方 SwordShield，剑盾兵无格挡反馈、满伤硬吃、被秒杀。

**根因（IL 实锤，Shield.ModifyAttack）**：
- 原版盾牌格挡分四类：近战（`monoAttacker is CloseCombatBrain`）、箭矢（`Shootable`）、飞斧（`ThrowingAxe`）、**长矛（`monoAttacker is Spear`）→ ×0.2**。
- 黑矛兵突刺的 `monoAttacker = Swordsman`（是 CloseCombatBrain）→ 走"近战"分支：**非 parry（parryAnim=false）时不减免伤害**（只播 Deflect/Block 音效+火花），parry 需 `MaybeParry` 且时序苛刻。
- 黑矛兵冲锋/爆发的 `monoAttacker = SpearChargeComponent` → **四类全不匹配** → 无反馈、无减免。
- → 我方剑盾兵被黑矛长矛命中 = 满伤硬吃 = "直接死亡"。

**修复**：新增 `TryShieldBlockSpear(target, ref atk)`，在 `DealSpearDamage`（突刺）/ `DealChargeDamage`（冲锋命中）/ `ArrivalBurst`（抵达爆发）三处结算前统一调用：
- 目标有 `Shield` 组件 + `agent.shield` 举起 + `Dot(shield.forward, -attack.direction) > 0.5` → 判定格挡。
- **伤害 ×0.2、眩晕 ×0.4**（与原版"长矛×0.2"同口径）。
- 反馈：冲锋/爆发（monoAttacker 非 CloseCombatBrain，原版不识别）由本组件播 `Sfx/English/SwordShield/Block` 音效 + `shieldSmash` 火花（设到 `atk.effect`）；突刺（Swordsman）原版 Shield 自己会播 Deflect/Block + 火花 → 只减免避免双音效。
- 日志：`[盾牌] 黑矛长矛被格挡 target=… facing=… dmg→0.42`。

### 3. 技能释放期可被击杀（平衡性，SpearChargeComponent.cs）

- `IAttackResponder.ModifyAttack` 删除 `attack.ignore=true`（冲锋/后退不再免疫伤害）——技能期间照常吃箭矢/近战/眩晕/击退，**可能被击杀**。保留 IAttackResponder 注册，想回调"冲锋霸体"只需在空实现里加 `if (_phase==Charging) attack.ignore=true;`。
- `Update()` 加死亡守卫：`aliveState` 失活立即 `AbortCharge()`（释放 exclusives、恢复 maxSpeed/movability、回 Idle），避免尸体继续推进 navPos。
- 已知残余：冲锋中 transform 追 navPos 仍有 ~0.9~1.6m 滞后（Body 动画速度上限），但命中判定用视觉长矛（transform）不受影响，且 `EndCharge` 安全网已消除结束回弹——非本轮重点。

### 4. 抽动探针去噪（TwitchProbe.cs）

- ⑥"精灵帧闪动"旧口径把**正常走路/跑步帧循环**误报成闪动（1s ≥3 帧名对移动单位恒真）→ 刷屏且掩盖真异常。
- 新口径：Body.stepping 激活 / navPos↔transform 位移 >0.05m / 上一帧位移 >0.02m / WindUp-Charging-Retreat 阶段 → 一律跳过 ⑥；只有**静止/站桩但精灵帧乱跳**才报警（这才是真·闪亮），日志前缀 `⑥精灵帧闪动(静止时)`。

### 本轮改动文件

| 文件 | 改动 |
|---|---|
| SwordRemover.cs | 预擦除传源纹理（修复空转根因）；`_preErased`→`_preErasedTex`（按源纹理）；预擦像素写共享克隆 |
| SpearChargeComponent.cs | 新增 `TryShieldBlockSpear`（突刺/冲锋/爆发三处调用）；ModifyAttack 删除免疫（技能期可击杀）；Update 死亡守卫 + `AbortCharge` |
| TwitchProbe.cs | ⑥精灵帧闪动只在静止时判定（移动/冲锋阶段跳过） |
| README.md / 本文件 | 第十九轮记录 |

**构建/部署**：0 警告 0 错误，Debug **126976B**，SHA `73E9D5DC…`；已部署 plugins 并哈希验证 MATCH；`.bak_r18`（125440B，第十八轮 DLL）已生成。
**待实测**：① `[去剑] 预擦除 … N 张` N>0、战斗/冲锋全程无剑刃闪回；② 黑矛突刺/冲锋/爆发打我方剑盾兵 → `[盾牌] 黑矛长矛被格挡 … dmg→0.42` + 盾击音效，剑盾兵不再被秒；③ 黑矛兵 WindUp/Charging 途中可被射杀/近战击杀，死亡后冲锋状态机干净收尾；④ `[抽动]` ⑥不再刷屏（仅静止帧乱跳报警）。

---

## 第二十轮（2026-08-16）：身子闪烁根治 + 剑柄改色应用诊断

**用户实测反馈（第十九轮日志 + 游戏内）**：黑矛兵身子仍闪烁（`[抽动]` ⑥在待机/冷却持续刷屏，1s 内帧名变化 4~7 次）；刺击盾牌格挡 OK；剑柄改身体色未生效（部件贴图克隆已改色但游戏内剑柄仍可见）。

### 1. 身子闪烁根因 = 去剑纹理 vs 网格 UV 错位一帧（SwordRemover.cs）

**证据链**：
- Unity 动画系统在 `Update → LateUpdate` 之间的内部动画步把 `SpriteAnimator.sprite` 字段推进到**下一帧**（Cecil 扫描确认：C# 代码无人写 `sprite` 字段，只有 `SpriteAnimator.SetSprite()` 读它 → 该字段由动画剪辑的对象引用绑定写入）。
- 原版 `SpriteAnimator.Update()` → `SetSprite()`：`lastSprite != sprite` 时 `bSprite = sprite`（同步网格 UV + `_MainTex`=原图集 + ComittBlock）。它**下一帧**才提交"动画刚写进字段的那一帧"。
- 旧代码 `SwordRemover.LateUpdate` 直接用 `_sa.sprite`（动画已推进到新帧）→ 把**新帧去剑纹理**盖到**旧帧网格 UV** 上 → 每换一帧动画闪一帧。待机 4~7 帧/秒 = 每秒闪 4~7 次；冲锋高帧率 = 频闪。
- **修复**：`SwordRemover` 新增 `Update()` 采样 `_frameSprite`（与原版 SetSprite 同一时刻同一值），`LateUpdate` 只用 `_frameSprite` 覆盖 `_MainTex`。去剑纹理与网格 UV 始终同帧 → 闪烁消除。

### 2. 抽动探针去噪（TwitchProbe.cs）

- ③"动画倒退"旧口径把**正常循环回卷**（Run clip 0.95→0.02）误报 → 改为只报 `_prevNorm<0.7` 时的中途倒退。
- ⑥"精灵帧闪动"旧口径把**正常待机呼吸循环**（帧按序前进）误报 → 改为"同一动画进度(norm 差<0.02)出现不同帧名"才算真闪动。

### 3. 剑柄改色应用诊断（SwordRemover.cs）

- 部件贴图克隆**已**改色（`剑柄改色=2142px`、残留诊断 `暗灰剑柄=0`），但游戏内剑柄仍可见 → 需判定"改色克隆是否真的上块"。
- 新增每只一次 `[去剑] sprite2 应用诊断`：打印 sprite2 是否已是 `_NoSword` 克隆 + 渲染器块 `_PartTex` 实例 ID 是否 == 克隆 ID。
- 判读：块里是克隆 → 改色已上块，剑柄仍可见 = 改色目标色 (33,26,24) 与运行时身体着色不符，下一步按运行时身体像素采样改色；块里是原部件贴图 → `SetSprite2` 被烘焙/重置覆盖，需查 `SpriteAnimator` 重烘焙路径。

### 本轮改动文件

| 文件 | 改动 |
|---|---|
| SwordRemover.cs | 新增 `Update()` 采样 `_frameSprite`；`LateUpdate` 改用 `_frameSprite` 覆盖 `_MainTex`（去剑纹理与网格 UV 同帧）；新增 sprite2 应用诊断 |
| TwitchProbe.cs | ③ 只报剪辑中途倒退；⑥ 改为"同动画进度换帧"才报 |
| README.md / 本文件 | 第二十轮记录 |

**构建/部署**：0 警告 0 错误，Debug **128000B**，SHA `79C5527F…`；已部署 plugins 并哈希验证 MATCH；`.bak_r19`（126976B，第十九轮 DLL）已生成。
**待实测**：① 战斗/冲锋/待机全程无身子闪烁；② `[抽动]` ③/⑥ 不再刷屏；③ `[去剑] sprite2 应用诊断` 判定改色克隆是否上块 → 决定剑柄下一步。



