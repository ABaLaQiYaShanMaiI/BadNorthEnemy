# 黑矛兵 → 混编战术小队 转型计划

> 状态：**转型成功（2026-08-18 实测确认）**——混编小队（盾/矛/弓）同船生成、各角色逻辑正常运行；两处架构假设已修正并对照原版核实。
> 背景：用户对比维京长矛项目后提出转型——不再只有"黑矛兵"，而是**一船三兵种混编战术小队**：
> 剑盾兵稳住阵脚、长矛兵冲锋回撤后在盾后刺击、弓箭手后排射击；全队仍是**一个小队**，遇到敌人**排起队列**。
> 本文档 = 可行性结论 + 架构修正 + 落地操作 + 里程碑路径；**落地代码在独立插件 BadNorthMixedSquad1.0，不动 v1.3**。

## ✅ 实施状态（2026-08-18）

- **第 1 步（fork）完成**：`BadNorthMixedSquad1.0/` 已建（GUID `badnorth.mixedsquad.v1.0`、单位 `Viking_MixedSquad`、bounty 12、4:3:2 可配），Release 构建 0 警告 0 错误，已部署 plugins。
- **M1（混编生成）完成**：
  - 注册混编引用：标称 viking = 原版 SwordShield（盾模板，不剥离）；长矛模板 = 黑矛兵剥离模板（黑矛兵定型）；弓模板 = 原版 Archer（type 查找）。
  - `Squad.CreateAgent` 补丁：前缀按**每船角色队列**换 prefab（盾/矛/弓），后缀挂 `MixedRole` 标记。
  - `On.Landing.Spawn` 前缀 `ComputeRoleQueue`：按本船混编 ShipLoad 的 count × 4:3:2 生成队列；postfix 只对 `MixedRole==Spear` 应用黑矛兵全套（盾/弓保持原版）。
  - 日志：`[混编] 本船角色统计 盾=x 矛=y 弓=z`（验证比例）。
- **新坑记录**：`VikingAgent : AgentComponent`（**不是** Agent）→ CreateAgent 需要的是 `Agent` 组件（`vikingRef.agent` / `x.agent`），模板缓存用 `.agent`。
- **M2（战术分层站位）完成**：
  - 新建 `TacticalFormation.cs`（挂在混编 Longship）：盾前/矛中/弓后三列 + 防御状态机。
  - HOLD（敌 >14m）：全队走向格位、朝敌站桩（`walkDir=0` + `LookInDirection`，覆盖大脑 = 压制冲建筑）。
  - ENGAGE（敌 ≤14m）：**盾兵保持前排钉位顶线**（原地近战=稳住阵脚），**矛/弓放开**各自作战（冲锋/射击）。
  - 死亡收拢（存活重排）；登岛 onMain 后才激活（船上让 Pirate 正常下船）。
  - cfg `EnableFormation`（默认 true；false=退化为原版各自冲建筑）。
- **M3（隔离加固 + 完整战术）完成**：
  - **隔离加固（用户要求"不干扰原版生成"）**：新增 `_inMixedLanding` 门控——`Squad.CreateAgent` 补丁**只在混编 Landing.Spawn 进行中**生效（前缀换 prefab + 后缀挂 MixedRole 都受门控），其余任何原版兵种生成一律不碰；`vikingClone` 未就绪时缓存 `_mixedNominal`，杜绝 `VikingReference.agent` 的 NPE（该属性无空校验）。
  - **弓手后排**：ENGAGE 期弓手钉后列（后排射击不贴脸）。
  - **冲锋回退联动**：`SpearChargeComponent.StartRetreat` 优先回退到本船阵型中列格位（`TacticalFormation.GetFormationSlot`，静态注册表查询），否则退回 0.6m。
  - **盾/弓保持原版**：不加血量倍率等改动（与"和原版一致"要求一致）。
- **代码审查修复（用户审查，2026-08-18）**：
  - **P1 真实 bug**：`_lastRole` 前缀串扰——同一混编船混装原版 ShipLoad 时，原版 agent 会继承上一个混编角色被误挂 MixedRole。修复：前缀每次调用开头 `_lastRole = None`，只有消费角色才赋值。
  - **P2**：今晚隔离测试——v1.3 DLL 已临时改名 `.bak_isolate`（只加载混编插件）；恢复：改回 `.dll`。
  - **P3**：`[PATCH·总览]` 补 `Mixed=OK/FAIL`；`[配置]` 日志改显示盾:矛:弓比例 + EnableFormation。
  - **P4**：命名残留清理（`[BS v1.3]` → `[MixedSquad]`）。
  - **P5 观察项**：混编矛兵吃黑矛兵数值（DamageMult 1.6 等）、盾/弓原版数值 → 混编船战力偏高，若碾压从 Bounty 或矛兵单独降倍率入手；3-4 矛兵齐冲压迫感待实测。
- **✅ 实测确认（2026-08-18，用户游戏日志）——转型成功**：
  - `[PATCH·总览] ... Mixed=OK`（CreateAgent 角色轮换补丁生效）
  - `[混编] 本船角色统计 盾=1 矛=1 弓=1（共 3）`（小船 3 人按比例分配）
  - 长矛全套正常：`[渲染] 长矛换皮肤`、`[Charge] 举矛/冲刺/命中/抵达爆发`、`[Charge] 开始后退 目标=(…)`（回退目标联动）、`[近战] 突刺/刺击`、死亡/尸体烘焙正常
- **🔧 修复：CreateAgent 补丁静默失效（实测发现）**：
  - 根因：`PatchMixedSquad` 用 `m == null` 判空反射对象 → .NET 3.5 Mono 无 `MethodInfo.op_Equality` → `MissingMethodException` → 补丁静默失效（Mixed=FAIL）。
  - 修复：`ReferenceEquals(m, null)`（坑清单第 1 条自己违规的教训）。`[PATCH·总览] Mixed` 字段第一时间暴露了它。
  - 附带：日志文件独立命名 `BadNorthMixedSquad1.0.log`（不再写 v1.3 的文件）。
- **🔧 修复：长矛换肤被动画还原（用户实测发现蓝臂残留）**：
  - 根因：`ApplySpearSkin` 用 `ReferenceEquals(bs.bSprite, skin)` 判断"已换过"——但长矛动画直接改 `_spriteRenderer.sprite`（绕过 bSprite），
    渲染精灵被改回原图时 bSprite 仍是陈旧皮肤 → 误判已换 → 跳过重换 → 英军蓝臂原图残留。
  - 修复：改判**当前渲染精灵** `ReferenceEquals(current, skin)`（`current = GetSpriteOf(bs)`），动画还原时每帧自动重换。v1.3 同款潜在 bug 一并修。
  - ⚠️ 注意：OneDrive 下增量构建可能漏编译改动 → 用 `dotnet build --no-incremental` 强制重建后再部署（本次哈希 C81AE4F1）。
- **✅ 手臂颜色问题已恢复**（用户实测确认，上述修复生效）。
- **🔧 遗留问题 1：原生剑未被处理（2026-08-19 已修复）**：
  - 现象：混编矛兵（黑矛兵）仍显示原生剑（剑盾基底烘焙在动画帧/部件贴图里的剑）。
  - **根因（代码级实锤，两处覆盖缺口）**：
    1. **部件贴图分区压暗被 `RemoveSword` 门控**：`SwordRemover.LateUpdate` 里 `if (_eraseEnabled) ApplySprite2Erase();`——
       `RemoveSword` 默认 `false` 时，`PartTex_SwordShield` 里的剑/剑柄亮银区**完全没被压暗**，剑经
       "身体帧像素 UV 编码 → 采样部件贴图"的渲染路径上屏 = 上屏仍有原生剑。
    2. **模式2 按部件名字判定 `swordShieldPart`**：若运行时部件名被烘焙/克隆改名（不含 "SwordShield"），
       会落入旧基底 `GetErasedSprite2`（亮银擦除 + 35% 安全阀）——SwordShield 部件亮银占比实测 40.8% > 35%，
       安全阀必拒 → 部件完全未处理 → 剑残留。
  - **修复（只改混编插件 `BadNorthMixedSquad1.0`，v1.3 不动）**：
    - `ApplySprite2Erase()` **解耦** `_eraseEnabled`：模式2 分区压暗**始终执行**（不依赖 `RemoveSword`）；
    - `ApplySprite2Erase` 改为**直接按 `Sprite2Mode` 处理**（`swordShieldPart` 名字只用于诊断日志），
      模式2 无条件生效，绕开"名字判定 → 旧基底安全阀拒绝"的坑；
    - 新增 `WritePartTexToAllBodyRenderers()`：帧擦除关闭（`RemoveSword=false`）时把压暗部件兜底写入
      全部 ColoredCharacter 渲染器块（`SetSprite2` 的 ComittBlock 只提交给 SpriteAnimator 自己的 rends，
      静态主身/镜像块的 `_PartTex` 仍是原件 → 剑仍会经它们上屏）；
    - `RemoveSword` 默认 `false → true`（预擦除 129 张 = 58 Onehanded + 71 Swordsman 已实测生效，
      安全阀防误擦）；启动日志 `[配置]` 与挂载日志 `[去剑] 挂载完成` 补上去剑配置便于实测验证。
  - **验证方式（下次实测）**：启动日志应见 `[去剑] 挂载完成: RemoveSword=true Sprite2Mode=2 ...（部件贴图分区压暗不依赖 RemoveSword）`；
    混编矛兵上屏不再有剑；若仍见剑 → 看 `[去剑] sprite2 应用诊断 ... 已是改色克隆=True | 匹配克隆=N/N` 是否全匹配
    （不匹配 = 烘焙重置部件贴图，按 v1.3 同款"可重入"应自动恢复）。

---


## 一、目标行为拆解（用户需求 → 行为项）

| # | 需求 | 行为拆解 |
|---|---|---|
| 1 | 生成长矛兵的同时生成剑盾兵和弓箭手 | 同一 shipGroup/Squad 内混编三种兵种（不再是单兵种 Squad） |
| 2 | 混搭在一艘船上 | 每艘 Longship 各自按比例装载盾/矛/弓（ship.agents = 该船的人） |
| 3 | 剑盾兵稳住阵脚 | 盾兵前排列，Swordsman 接敌 + Shield 格挡 |
| 4 | 长矛兵冲刺后回退至阵后 | 冲锋 → 回退 → 回**中列**阵位 |
| 5 | 在保护下刺击敌人 | 站在盾后一列用长矛刺击（射程 0.69m ≫ 剑 0.118m，够得着盾前敌） |
| 6 | 弓箭手在后面射击 | 弓手钉**后列**，敌人进射程才射，逼近则后退 |
| 7 | 仍旧是小队 | **一个 shipGroup = 一个 Squad**（同波所有船共享），共生死天然成立 |
| 8 | 遇到敌人则排起队列 | 上岛 → 抵阵位 → 对敌方向**分层列队**（盾/矛/弓三列）→ 敌人进范围才交战 |

---

## 二、可行性结论：✅ 能实现，积木全部现成或已验证

| 目标 | 积木 | 来源/依据 |
|---|---|---|
| 混编同小队 | `Squad.CreateAgent(Agent prefab, NavPos)` 的 **prefab 是参数**，Patch 按角色换 prefab | 原版 Squad.cs:161；维京长矛 `Squad_CreateAgent_Patch` 已跑通 |
| 盾兵稳线 | SwordShield 基底（Shield 组件 + Swordsman 大脑） | 原版 |
| 长矛冲锋+回退+刺击 | `SpearChargeComponent`（冲锋→回退→近战刺击；射程 0.69m 可从盾后够到盾前敌） | 本项目已实现 |
| 弓手射击 | Archer 类型 + `Archery` 组件（射程攻击，`enemyDist` 有近距逻辑） | 原版 |
| 队列数学 | `SquadFormation` 结构体（网格 + 蛇形编号 + 朝向旋转 + 余数处理） | 原版 SquadFormation.cs（当前只挂 EnglishSquad，需复用到敌方） |

---

## 三、两处架构修正（影响代码，已对照原版核实）

### ⚠️ 修正 1：一个 shipGroup = 一个 Squad（不是"一艘船"）

敌方生成链（原版 `Landing.cs:195-234`，已核实）：

```
Landing.Spawn()
  → Squad squad = this.shipGroup.squad        (Landing.cs:201)   ← 组级共享小队
  → 每个 ShipLoad 的每个 agent：
      squad.CreateAgent(shipLoad.vikingRef.agent, navPos)   (:219)
      agent.GetOrAddComponent<Pirate>().AddToLongship(ship) (:220)
      agent.GetOrAddComponent<VikingAgent>().vikingReference = shipLoad.vikingRef (:221)
```

- `ShipGroup.squad` 是懒加载的（`ShipGroup.cs:53-63`：`if (!this._squad) this._squad = vikingSquad.SpawnGetFromPrefab(...)`），**同一波（shipGroup）的所有船共享同一个 Squad**。
- 推论：
  - **"共生死"天然成立**（同组共 squad），不用做任何事；
  - **阵型锚点不能挂 Squad**（跨船会串）→ 挂 **Longship / 该船登陆点** 层面——正好是现有 `OnLandingSpawn` 逐船 hook 的位置，`ship.agents` 就是该船的人，直接用；
  - 混编事件/角色记录要按 `agent.vikingReference == 混编引用` 过滤（多船共享同一 squad，事件跨船触发）。

### ⚠️ 修正 2：SquadCoordinator 是玩家侧类，敌方没有实例

已核实 `SquadCoordinator.cs`：抽象 MonoBehaviour，`Setup(Squad)` 里订阅 `squad.onAgentCreated / onAgentRemoved`（`OnAgentCreated/OnAgentRemoved` 虚方法）。子类如 SwordSquadCoordinator 是玩家侧追踪器；**敌方 Viking 侧没有任何 Coordinator 实例**，不能"继承挂到敌方"。

做法：**借鉴它的事件订阅模式**，自建混编管理组件（挂到 shipGroup.squad 或自己的管理对象上），订阅 `squad.onAgentCreated` 记录角色；注意多船共享同一 squad → 用 `agent.vikingReference == 混编引用` 过滤。

---

## 四、决定：新建独立插件 BadNorthMixedSquad1.0（不动 v1.3）

- **新建目录 `BadNorthMixedSquad1.0/`**（新 GUID `badnorth.mixedsquad.v1.0`、新单位名 `Viking_MixedSquad`），把 v1.3 整体复制过去再改造。
- v1.3 是 48 轮闭环定稿；混编（尤其防御状态机）是原版没有的全新行为，风险最高——**保留 v1.3 可随时回滚对比**。
- 两插件可并存：v1.3 的 `Viking_BlackSpearman` + 新单位 `Viking_MixedSquad` 互不冲突；想"替换"黑矛兵就只启用新版。

---

## 五、三块落地操作（对应里程碑 1→2→3）

### 块 1：混编生成（M1，工作量小，直接抄维京长矛）
- 抄 `Squad_CreateAgent_Patch.cs` 改成**角色轮换**版：
  - `EnsureTemplate()` 模板构建：盾模板 = 原版 `Viking_SwordShield` 克隆；矛模板 = 黑矛兵剥离模板（现有 `BuildStrippedTemplate` + `SpearChargeComponent` 逻辑）；弓模板 = 原版 `Viking_Archer` 克隆（保留 Archery + Archer 大脑，剥离 Arsonist 等）。
  - `Prefix`：`prefab == _registeredCloneAgent`（= `vikingRef.agent`，是 vikingClone 的 **agent 属性**，不是按名字）时，按该船角色队列换 prefab。
- **每船精确比例分配**：`OnLandingSpawn`（`On.Landing.Spawn` 包装器）在 `orig(self)` 之前读 `self.shipLoads` 里混编 ShipLoad 的 count，按 **4:3:2** 生成序列存静态字段 + cursor；`CreateAgent` Prefix 消费它。`orig` 内 CreateAgent 同步调用 → 静态字段安全。**别用全局轮询**（船数非 9 倍数时会缺兵种）。
- 注册：照 `RegisterBlackSpearman` 抄一份，viking 字段指向盾兵模板，type 复用 SwordShield，bounty 先给 9~12（9 人混编）。
- 给每个生成 agent 挂 **MixedRole 标记组件**（Role 枚举：Shield/Spear/Archer），供块 2/3 识别。

### 块 2：战术分层站位（M2，核心新组件）
- 新建 `TacticalFormation.cs`（MonoBehaviour），挂到每艘混编 Longship（`OnLandingSpawn` 里 `Setup(ship, agents)`）。
- 每帧：
  - `new SquadFormation(count, bounds, enemyDir).Get(index)` 做网格分布（ctor(int,Bounds,Vector3) + Get(int)→Vector3：蛇形+朝向旋转+余数处理全在），再加**分层偏移**（盾=前排、矛=后 1 列、弓=后 2 列）；
  - 钉位：`walkDir = 0` + `LookInDirection(enemyDir)`（站桩写法直接抄维京长矛 `VikingSpearBrain.BraceUpdate`）+ NavPos 移到格位；
  - 存活过滤重算 index = **死亡收拢**。
- **防御状态机（船级）**：登岛 → 抵阵位（敌距离 > 阈值：站桩朝敌）→ 敌进范围 → 交战（放开各 Brain）。
- ⚠️ **最大不确定性（计划风险 1）**：原版 `Pirate` 组件驱动敌人冲建筑，需压制（拦截 Pirate 移动或在钉位逻辑覆盖）。**M2 最先攻克此点。**

### 块 3：弓手后排 + 完整战术（M3）
- 弓手钉后列；"逼近后退"约束：参考原版 `Archery.cs` 的 `enemyDist` 近距分支（:223/:263），或在阵型组件每帧把弓手往后拉。
- **冲锋回退落点联动**（计划风险 5）：`SpearChargeComponent._retreatEndPos` 现在是"起点反向 0.6m"（StartRetreat/DoRetreat），改成查询 `TacticalFormation` 里该矛兵的中列格位。
- **平衡**：比例 4:3:2 做成 cfg 可配，调 bounty / 血量倍率（盾兵可加血）。

---

## 六、直接可抄清单

| 需求 | 抄哪里 |
|---|---|
| prefab 交换混编 | 维京长矛 `Squad_CreateAgent_Patch.cs`（改角色轮换） |
| 站桩等敌 / 逼进退后 / 盾后刺击 | 维京长矛 `VikingSpearBrain.cs`（Approach/Brace/Stab，761 行） |
| 冲锋→回退→刺击 | 本项目 `SpearChargeComponent.cs`（改回退落点） |
| PNG 皮肤 / 去手臂 | 本项目 `BlackSpearmanVisual.cs` + 维京长矛 `SpearmanSkin.cs` |
| 网格数学 | 原版 `SquadFormation`（ctor(int,Bounds,Vector3) + Get） |
| 敌方生成链 | 原版 `RaidGeneration/Landing.cs:195-234`（squad=shipGroup.squad） |
| 注册 / 注入生成池 | 本项目 `Plugin.cs`（RegisterBlackSpearman + LevelNodeSetupPostfix） |

---

## 七、写代码前先背的坑

1. **.NET 3.5 雷区**：禁 `System.Action/Func`（TypeLoadException 会让整批 Patch 静默失效，v1.3 最贵教训）、`string.Join(IEnumerable)`、`FieldInfo == null` 判断——新代码全守。
2. **时序**：`agent.Setup(navPos)` 在 `onAgentCreated` 之前 → `AddComponent` 挂的组件不会被 `Brain.Setup` 收集 → **角色组件要在模板 prefab 上就挂好**（维京长矛 BuildTemplate 就是这么干的），或手动 `RegisterBrainAction`。
3. **弓手大脑是 Archer 不是 Swordsman**——矛兵刺击 Patch 的是 Swordsman 方法，**弓模板别带 Swordsman 组件**。
4. **混编判定用 `prefab == vikingRef.agent`**（vikingClone.agent 属性），不是按名字。
5. **squad 事件跨船串**：`squad.onAgentCreated` 用 `agent.vikingReference` 过滤。
6. **Pirate 冲建筑要压制**（计划风险 1）。
7. **平衡**：混编总战力 > 单兵种，bounty/倍率必须调（计划风险 4）。

---

## 八、风险与取舍

1. **"排起队列等敌"是全新行为**：原版敌人没有防御阵型概念，防御状态机需自建；且原版 `Pirate` 组件驱动敌人冲建筑，必须压制——这是最大不确定性。
2. **Squad.prefab 单兵种假设**：Squad 的命名/补充/UI 路径假设单 prefab；混编需容忍（敌人侧影响可控，不涉及玩家补给）。
3. **弓手前排化**：原版弓手会贴脸射击，需加"远离前排"约束，否则阵型失效。
4. **混编的平衡**：三兵种同队 = 总战力提升，需调 bounty/配额/血量倍率，避免碾压。
5. **冲锋与阵型的冲突**：长矛冲锋会脱离阵线（穿透+回退 0.6m），回退落点必须精确回到中列，否则打乱阵型——冲锋触发/落点要跟格位联动。

---

## 九、推荐顺序

1. 新建目录 + 复制 v1.3 + 改 GUID/单位名 → 构建部署确认能跑。
2. **M1**：CreateAgent 角色轮换 + 三模板 → 实测"一船盾+矛同船登岛、矛冲锋→回退→盾后刺击"。
3. **M2**：TacticalFormation + 防御状态机 → 实测"遇敌排三列、盾线顶住、矛回中列"。
4. **M3**：弓手 + 平衡 → 实测"弓手后排射击不贴脸"。

每步沿用现有传统：开发日志 + `[混编]/[阵型]/[冲锋]` 关键词日志 + build.ps1 SHA 校验。

---

## 十、验证方式

- 每里程碑构建部署后进游戏实测；日志按关键词分类（[混编]/[阵型]/[冲锋]）。
- 关键观察点：登岛后是否抵阵列队、盾线是否顶住、长矛冲锋回退是否回中列、弓手是否保持后列射击。
- 阵型像素验证可复用 [像素采样]/[头部统计] 类探针思路（如需）。

---

## 十一、附录：关键代码事实（已核实）

| 事实 | 位置 |
|---|---|
| `Landing.Spawn()`：`Squad squad = this.shipGroup.squad`，逐 agent `CreateAgent(vikingRef.agent)` + Pirate + vikingReference | 原版 `RaidGeneration/Landing.cs:195-234`（:201/:219/:220/:221） |
| `ShipGroup.squad` 懒加载（同波共享一个 Squad） | 原版 `RaidGeneration/ShipGroup.cs:53-63` |
| `SquadCoordinator` 是玩家侧抽象类，订阅 onAgentCreated/onAgentRemoved；敌方无实例 | 原版 `SquadCoordinator.cs` |
| `Squad.CreateAgent(prefab, navPos)` prefab 是参数，可 Patch 换 | 原版 `Squad.cs:161` |
| `SquadFormation` 网格/蛇形/旋转数学（现只给英军用） | 原版 `SquadFormation.cs`，使用方 `EnglishSquad.cs:230`、`NavSpotFormationSquad.cs:126` |
| 弓手射程攻击组件 + enemyDist 近距分支 | 原版 `Archery.cs`（:223/:263） |
| 混编生成已有人跑通 | 维京长矛 `Squad_CreateAgent_Patch.cs`（prefab 交换） |
| 本项目长矛冲锋/回退/刺击 | `SpearChargeComponent.cs`（Charge→Retreat→Thrust） |
