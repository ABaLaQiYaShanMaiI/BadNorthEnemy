# 新增敌方单位 · 完整技术方案与路径对比

> **撰写日期**：2026-08-09  
> **目标**：在 Bad North 中真正「新建」一个敌方单位（而非修改现有单位），并将其注入敌方生成池，使其能被正常创建到游戏中。  
> **前置分析**：基于 BadNorthDatabase-main 全部源码 + BadNorthEnemy-main 全部版本（v1.0/v1.1/v1.18）+ BBB / PlentyTraits 参考模组。

---

## 1. 核心约束分析 —— 为什么 DLL 这么难？

### 1.1 不可逾越的硬约束

| 约束层 | 具体内容 | 是否可突破 |
|--------|----------|------------|
| **VikingAgent.Type 枚举** | 8 个值硬编码在 `Assembly-CSharp.dll` 中：`Sword(0), SwordShield(1), Archer(2), Tank(3), Beserker(4), AxeThrower(5), TankArcher(6), Twohanded(7)` | ❌ 编译时固定，无法运行时新增枚举值 |
| **VikingReference 预制件** | 每个敌种类对应一个 Unity GameObject，挂载 `VikingReference` + `LevelRule` + `LevelGuessable` 等组件，打包在 AssetBundle 中 | ⚠️ 无法从零创建完整预制件（缺少 Animator/Mesh/Material 烘焙数据） |
| **AI Brain 类绑定** | 每个 `VikingAgent.Type` 在预制件上绑定特定 Brain（`Swordsman` / `TankBrain` / `Archery`），类名编译在 Assembly-CSharp 中 | ❌ 无法创建全新的 Brain 类 |
| **渲染管线** | `BatchedSprite` / `SpriteAnimator` / `AgentTextureBaker` 是紧密耦合的渲染链，纹理 UV 编码在编译时确定 | ⚠️ 可运行时修改颜色，但武器替换极其复杂 |

### 1.2 敌人完整生成链路（源码验证）

```
GameSetup.Awake()
  └─ IGameSetup.OnGameAwake() 遍历所有 MonoBehaviour
       └─ VikingReference.OnGameAwake()
            └─ LevelStateObjectReferences.AddToDict(this)   ← 注册到全局字典

战役生成阶段：
  Campaign → RaidGenerator.OnCampaigCreation()
    └─ 设置 per-level: wavesCount / bountyPerWave / enemyTypes
    └─ ArcConsistency 约束求解器 (LevelRule / LevelGuessable)
         └─ 筛选出本关可用的 VikingReference 集合

关卡加载阶段：
  Raid.GenerateRaid()
    └─ 从 possibleAgents 生成 ShipGroup → Landing → ShipLoad
         └─ ShipLoad.vikingRef → VikingReference 引用

实际生成阶段：
  Landing.Spawn()
    └─ Instantiate(shipLoad.vikingRef.viking)   ← 克隆预制件
    └─ 或 Instantiate(shipLoad.vikingRef.vikingClone)
```

### 1.3 当前 BlackSpearman 所有版本的问题根源

| 版本 | 策略 | 核心问题 | 状态 |
|------|------|----------|:---:|
| ~~v1.0/v1.x (旧)~~ | 已删除 — 与 v1.0 完全一致（仅插件 ID 不同） | — | 🗑️ 已删除 |
| v1.0 | 创建新 VikingReference 注册到 dict + 武器系统 | ❌ 时序问题（dict 在 Awake 时为空）+ 武器提取时机不稳定 | 🔒 **已封存** |
| v1.1 (BBB风格) | 劫持 `Viking_SwordShield` 的 VikingReference，替换 `viking` 字段 | ⚠️ SwordShield 消失 + 所有 SwordShield 都变成黑矛兵 | 🔒 **已封存** |
| ~~v1.15~~ | （中间迭代版本） | — | — |
| ~~v1.18~~ | 从 Pikeman 预制件提取武器 + sprite2 替换 + 冲刺技能 | ❌ 复杂度爆炸 | — |

> **⚠️ 重要通知（2026-08-09）**：
> 
> **BadNorthBlackSpearman1.0 和 BadNorthBlackSpearman1.1 已正式封存，不再继续开发。**
> 
> 原因：
> 1. 运行时 DLL 方案受限于 `VikingAgent.Type` 枚举不可扩展的硬约束
> 2. 无论怎么优化反射和 Hook，都无法从根本上解决「枚举值冒充」问题
> 3. 武器替换（BatchedSprite 克隆）的渲染管线耦合过于复杂
> 
> **新方向**：转向 §10-13 中描述的 **D + F 外部工具链方案**（Mono.Cecil 枚举补丁 + UnityPy Asset 补丁），从 IL 层面和资源文件层面一次性解决问题。


---

## 2. 路径 A（推荐）：DLL — 最小化「借用预制件 + 克隆注入」法

### 2.1 核心思路

> **不创建全新预制件，而是：克隆一个现有 VikingReference → 修改关键字段 → 以新名字注册到 dict → Hook Spawn 时注入自定义组件。**

这是 ~~v1.1 BBB 风格~~ 的正确进化版——不再劫持原版单位，而是克隆一份。**（注意：v1.0/v1.1 已封存，此方案为新设计，不沿用旧代码。）**

### 2.2 完整实现方案

```csharp
// ==================== Plugin.cs 核心架构 ====================
using System;
using System.Collections;
using System.Collections.Generic;
using System.Reflection;
using BepInEx;
using HarmonyLib;
using UnityEngine;
using Voxels.TowerDefense;
using Voxels.TowerDefense.RaidGeneration;
using Voxels.TowerDefense.CampaignGeneration.CampaignAc3;

namespace BadNorthNewEnemy
{
    [BepInPlugin("badnorth.newenemy", "Bad North - New Enemy", "1.0.0")]
    public class Plugin : BaseUnityPlugin
    {
        public static Plugin Instance;
        Harmony _harmony;
        
        // === 配置参数 ===
        const string SOURCE_VR_NAME = "Viking_SwordShield";    // 克隆源
        const string NEW_VR_NAME = "Viking_BlackSpearman";     // 新单位名称
        const VikingAgent.Type NEW_TYPE = VikingAgent.Type.SwordShield; // 复用枚举值
        const int NEW_BOUNTY = 8;
        const float DAMAGE_MULT = 1.6f;
        const float KNOCKBACK_MULT = 2.5f;
        const float ARMOR_MULT = 1.3f;
        const float SCALE_MULT = 1.05f;
        const float RANGE_MULT = 3.5f;
        
        // === 追踪已修改的 Agent ===
        internal static HashSet<Agent> ModifiedAgents = new HashSet<Agent>();
        
        void Awake()
        {
            Instance = this;
            _harmony = new Harmony("badnorth.newenemy");
            
            // 步骤1：Hook GameSetup.Awake — 在所有 IGameSetup 注册完成后注入
            On.Voxels.TowerDefense.GameSetup.Awake += OnGameSetupAwake;
            
            // 步骤2：Hook Landing.Spawn — 拦截 Agent 生成时机
            var spawnMethod = typeof(Landing).GetMethod("Spawn", 
                BindingFlags.Instance | BindingFlags.Public | BindingFlags.NonPublic);
            if (spawnMethod != null)
                _harmony.Patch(spawnMethod, 
                    postfix: new HarmonyMethod(GetType().GetMethod("OnAgentSpawned",
                        BindingFlags.NonPublic | BindingFlags.Static)));
            
            // 步骤3：Hook Swordsman.GetAttack & range（数值强化）
            PatchCombatStats();
            
            Logger.LogInfo("[NewEnemy] Plugin initialized");
        }
        
        void Start()
        {
            InvokeRepeating("ScanForNewAgents", 0.5f, 0.5f);
        }
    }
}
```


```csharp
        // ================================================================
        // 步骤1：注入新 VikingReference 到全局注册表
        // ================================================================
        static void OnGameSetupAwake(On.Voxels.TowerDefense.GameSetup.orig_Awake orig, GameSetup self)
        {
            orig(self); // 先执行原版 Awake
            
            // 安全检查：是否已注入过
            if (LevelStateObjectReferences.dict.ContainsKey(NEW_VR_NAME))
            {
                Instance.Logger.LogInfo($"[NewEnemy] {NEW_VR_NAME} already registered");
                return;
            }
            
            // 获取源 VikingReference
            if (!LevelStateObjectReferences.dict.TryGetValue(SOURCE_VR_NAME, out var sourceObj))
            {
                Instance.Logger.LogError($"[NewEnemy] Source {SOURCE_VR_NAME} not found!");
                return;
            }
            
            var sourceVR = sourceObj as VikingReference;
            if (sourceVR == null)
            {
                Instance.Logger.LogError("[NewEnemy] Source is not VikingReference!");
                return;
            }
            
            try
            {
                // 克隆源 VikingReference 所在的 GameObject（含所有组件）
                var newGO = Instantiate(sourceVR.gameObject);
                DontDestroyOnLoad(newGO);
                newGO.name = NEW_VR_NAME;
                
                var newVR = newGO.GetComponent<VikingReference>();
                
                // === 关键字段修改 ===
                newVR.type = NEW_TYPE;
                newVR.bounty = NEW_BOUNTY;
                
                // === 修改 LevelRule: 出现条件 ===
                var levelRule = newGO.GetComponent<LevelRule>();
                if (levelRule != null)
                {
                    levelRule.condition.expression = 
                        "(fraction > 0.10 && fraction < 0.50) || (fraction > 0.65 && fraction < 0.95)";
                }
                
                // === 修改 LevelGuessable: 出现概率 ===
                var levelGuessable = newGO.GetComponent<LevelGuessable>();
                if (levelGuessable != null)
                {
                    if (LevelStateObjectReferences.dict.TryGetValue("Viking_AxeThrower", out var axeObj))
                    {
                        var axeVR = axeObj as VikingReference;
                        if (axeVR != null)
                        {
                            var axeGuessable = axeVR.GetComponent<LevelGuessable>();
                            if (axeGuessable != null)
                                levelGuessable.probability = axeGuessable.probability;
                        }
                    }
                }
                
                // === 注册到全局字典 ===
                LevelStateObjectReferences.dict[NEW_VR_NAME] = newVR;
                
                Instance.Logger.LogInfo($"[NewEnemy] ✅ {NEW_VR_NAME} registered! bounty={NEW_BOUNTY}");
                
                // === 延迟修改预制件（等待 Start 完成） ===
                Instance.StartCoroutine(ModifyPrefabDelayed(newVR));
            }
            catch (Exception ex)
            {
                Instance.Logger.LogError($"[NewEnemy] Failed to register: {ex}");
            }
        }
```

```csharp
        // ================================================================
        // 延迟修改预制件 — 等待 VikingReference.Start() 完成
        // ================================================================
        static IEnumerator ModifyPrefabDelayed(VikingReference newVR)
        {
            yield return new WaitForSeconds(2f);
            
            try
            {
                var vikingClone = newVR.vikingClone;
                if (vikingClone == null)
                {
                    Instance.Logger.LogError("[NewEnemy] vikingClone is null!");
                    yield break;
                }
                
                // 修改颜色为黑色
                RecolorBlack(vikingClone.transform);
                
                // 添加 Marker 组件（用于后续 Spawn 时识别）
                vikingClone.gameObject.AddComponent<BlackSpearmanMarker>();
                
                // 修改数值
                var agent = vikingClone.agent;
                if (agent != null) agent.scale *= SCALE_MULT;
                
                var swordsman = vikingClone.GetComponent<Swordsman>();
                if (swordsman != null)
                {
                    ScaleFloatArray(swordsman.damageLevels, DAMAGE_MULT);
                    ScaleFloatArray(swordsman.knockbackLevels, KNOCKBACK_MULT);
                }
                
                var armor = vikingClone.GetComponent<Armor>();
                if (armor != null)
                {
                    var armorField = typeof(Armor).GetField("armor",
                        BindingFlags.Instance | BindingFlags.Public | BindingFlags.NonPublic);
                    if (armorField != null)
                    {
                        var origArmor = armorField.GetValue(armor) as float[];
                        if (origArmor != null)
                        {
                            var newArmor = new float[origArmor.Length];
                            for (int i = 0; i < origArmor.Length; i++)
                                newArmor[i] = origArmor[i] * ARMOR_MULT;
                            armorField.SetValue(armor, newArmor);
                        }
                    }
                }
                
                Instance.Logger.LogInfo("[NewEnemy] ✅ Prefab modification complete");
            }
            catch (Exception ex)
            {
                Instance.Logger.LogError($"[NewEnemy] Prefab modification failed: {ex}");
            }
        }
```



        // ================================================================
        // 步骤2：定时扫描新生成的 Agent & 注入技能
        // ================================================================
        void ScanForNewAgents()
        {
            var allAgents = FindObjectsOfType<Agent>();
            foreach (var agent in allAgents)
            {
                if (agent == null || !agent.isViking || ModifiedAgents.Contains(agent))
                    continue;
                var marker = agent.GetComponent<BlackSpearmanMarker>();
                if (marker != null && !ModifiedAgents.Contains(agent))
                    ApplyRuntimeMods(agent);
            }
        }
        
        internal static void ApplyRuntimeMods(Agent agent)
        {
            if (ModifiedAgents.Contains(agent)) return;
            ModifiedAgents.Add(agent);
            var chargeComp = agent.gameObject.AddComponent<SpearChargeComponent>();
            if (chargeComp != null) chargeComp.Setup(agent);
            var stabAction = agent.gameObject.AddComponent<SpearStabAction>();
            var brain = agent.brain as Swordsman;
            if (brain != null)
            {
                try
                {
                    var actionsField = typeof(Brain).GetField("actions",
                        BindingFlags.Instance | BindingFlags.Public | BindingFlags.NonPublic);
                    if (actionsField != null)
                    {
                        var actions = actionsField.GetValue(brain) as System.Collections.IList;
                        if (actions != null)
                        {
                            if (chargeComp != null && !actions.Contains(chargeComp))
                                actions.Add(chargeComp);
                            if (stabAction != null && !actions.Contains(stabAction))
                                actions.Add(stabAction);
                        }
                    }
                }
                catch { }
            }
            Instance.Logger.LogInfo($"[NewEnemy] Agent {agent.name} modified");
        }


        // ================================================================
        // 步骤3：数值强化（Harmony Patch）
        // ================================================================
        void PatchCombatStats()
        {
            var getAttackMethod = typeof(Swordsman).GetMethod("GetAttack",
                BindingFlags.Instance | BindingFlags.Public | BindingFlags.NonPublic,
                null, new[] { typeof(Agent) }, null);
            if (getAttackMethod != null)
                _harmony.Patch(getAttackMethod,
                    prefix: new HarmonyMethod(GetType().GetMethod("GetAttack_Prefix",
                        BindingFlags.NonPublic | BindingFlags.Static)));
            
            var rangeProp = typeof(Swordsman).GetProperty("range",
                BindingFlags.Instance | BindingFlags.Public | BindingFlags.NonPublic);
            if (rangeProp != null)
            {
                var getter = rangeProp.GetGetMethod(true);
                if (getter != null)
                    _harmony.Patch(getter,
                        prefix: new HarmonyMethod(GetType().GetMethod("Range_Prefix",
                            BindingFlags.NonPublic | BindingFlags.Static)));
            }
        }
        
        static bool GetAttack_Prefix(Swordsman __instance, Agent target, ref Attack __result)
        {
            if (!ModifiedAgents.Contains(__instance.agent)) return true;
            int lv = __instance.agent.squad != null ? __instance.agent.squad.level : 0;
            float dmg = 2.5f, kb = 1.2f, stun = 6f;
            if (lv < __instance.damageLevels.Length) dmg = Mathf.Max(dmg, __instance.damageLevels[lv]);
            if (lv < __instance.knockbackLevels.Length) kb = Mathf.Max(kb, __instance.knockbackLevels[lv]);
            if (lv < __instance.stunLevels.Length) stun = Mathf.Max(stun, __instance.stunLevels[lv]);
            Vector3 dir = (target.chestPos - __instance.agent.chestPos).normalized;
            dir.y = 0f;
            if (dir.sqrMagnitude < 0.001f) dir = __instance.transform.forward;
            __result = new Attack(
                new AttackSettings(dmg, kb, 0f, stun),
                dir, (target.wChestPos + __instance.agent.wChestPos) / 2f,
                __instance, __instance.agent.squad, "Sfx/English/Spear");
            return false;
        }
        
        static bool Range_Prefix(Swordsman __instance, ref float __result)
        {
            if (!ModifiedAgents.Contains(__instance.agent)) return true;
            __result = __instance.agent.radius * 0.7f * RANGE_MULT;
            return false;
        }

        static void RecolorBlack(Transform t)
        {
            var sprites = t.GetComponentsInChildren<BatchedSprite>(true);
            if (sprites == null) return;
            var colorProp = typeof(BatchedSprite).GetProperty("color",
                BindingFlags.Instance | BindingFlags.Public | BindingFlags.NonPublic);
            if (colorProp == null) return;
            foreach (var bs in sprites)
            {
                if (bs == null) continue;
                try
                {
                    var c = (Color)colorProp.GetValue(bs, null);
                    colorProp.SetValue(bs, new Color(c.r, c.g, 0.01f, c.a), null);
                }
                catch { }
            }
        }
        
        internal static void ScaleFloatArray(float[] arr, float mult)
        {
            if (arr == null) return;
            for (int i = 0; i < arr.Length; i++) arr[i] *= mult;
        }
    }
    


### 2.3 路径 A 的优势

| 优势 | 说明 |
|------|------|
| ✅ **不劫持原版单位** | SwordShield 仍然存在，黑矛兵是独立的新条目 |
| ✅ **独立概率控制** | 可通过 LevelGuessable 独立调节黑矛兵的出现概率 |
| ✅ **独立出现区间** | 通过 LevelRule.condition 控制哪些关卡出现 |
| ✅ **无需武器替换** | 放弃复杂的 Pikeman 武器克隆，专注数值 + AI 行为差异化 |
| ✅ **最小化复杂性** | 核心逻辑 < 200 行，清晰可维护 |

### 2.4 路径 A 的局限

| 局限 | 影响 | 缓解方案 |
|------|------|----------|
| 复用 `Type.SwordShield` | 统计/UI 中与 SwordShield 无法区分 | 通过 Marker 组件区分；牺牲统计精度 |
| 无独立3D武器模型 | 外观仍是剑盾兵的 mesh | 颜色 + scale 区分（黑色 + 微放大） |
| 需定时扫描 Agent | 比 Hook Spawn 直接拦截多 0.5s 延迟 | 可改用更精确的 Harmony Patch Landing.Spawn |
| prefab 修改时机 | 需等待 Start() 完成 | Coroutine 延迟 2s 足够稳定 |

---

## 3. 路径 B：DLL — MonoMod IL 级别扩展

### 3.1 核心思路

> 使用 **MonoMod** 在 IL 层面修改 `VikingAgent.Type` 枚举，新增一个值（如 `BlackSpearman = 8`），同时修改 `numTypes = 9`。需要 Patch Assembly-CSharp.dll 的 IL 字节码。

### 3.2 可行性分析

| 操作 | 技术可行性 | 工具 |
|------|------------|------|
| 添加枚举字面量 `BlackSpearman = 8` | ✅ 可行（IL层面修改） | MonoMod / Mono.Cecil |
| 修改 `numTypes` 为 9 | ✅ 可行 | Mono.Cecil |
| 修改 `VikingReference` 关联 | ✅ 可行 | 运行时反射 |
| 修改 `RaidGenerator` / ArcConsistency | ⚠️ 复杂（枚举 switch 散落各处） | 大规模 IL Patch |
| 修改 `EnemyLineupIntro` UI | ⚠️ 需 Patch UI 组件 | 反射 + Hook |

**结论**：IL 级别扩展枚举值技术要求极高，且需 Patch 代码点极多（所有 `switch(type)` 处），**投入产出比极低**。

---

## 4. 路径 C：非 DLL 外置程序方案

### 4.1 方案 C1：Python + 内存编辑（pymem）

| 评估项 | 评分 |
|--------|------|
| 实现难度 | ⭐⭐⭐⭐⭐（需深入 Mono 运行时内部、托管堆布局） |
| 可维护性 | ⭐（每个游戏版本更新都可能破坏） |
| 稳定性 | ⭐（内存操作极易崩溃） |
| **结论** | ❌ **不推荐** — 仅在无任何 DLL 注入可能时考虑 |

### 4.2 方案 C2：预启动资源替换工具

工具程序（C# / Python）→ 游戏启动前扫描 AssetBundle → 解包 → 修改 prefab → 重新打包 → 替换原文件。

| 评估项 | 评分 |
|--------|------|
| 实现难度 | ⭐⭐⭐⭐（AssetBundle 逆向工程） |
| 依赖工具 | UABE / AssetStudio / UnityPy |
| 局限性 | 仍需枚举值限制；BinPatcher 风险 |
| **结论** | ⚠️ **理论可行**，但不能突破枚举限制 |

### 4.3 方案 C3：外部 AssetBundle 加载器 ✅ 推荐

```
最少 DLL（仅做 AssetBundle 加载）
    ↓
1. 在 Unity Editor 中创建完整 prefab（含 VikingReference + Mesh + Material + Animator）
2. 打包成 .bundle 文件
3. 放置到 BadNorth/BepInEx/plugins/NewEnemy/ 目录
4. 极简 DLL（~30行）在 GameSetup.Awake 时加载并注入
```

**Loader.cs 骨架**：
```csharp
[BepInPlugin("badnorth.newenemy.ab", "New Enemy Loader", "1.0")]
public class AssetBundleLoader : BaseUnityPlugin
{
    void Awake()
    {
        var bundlePath = Path.Combine(Paths.PluginPath, "NewEnemy/bundle");
        var bundle = AssetBundle.LoadFromFile(bundlePath);
        var prefab = bundle.LoadAsset<GameObject>("Viking_BlackSpearman");
        var vr = prefab.GetComponent<VikingReference>();
        
        On.Voxels.TowerDefense.GameSetup.Awake += (orig, self) => {
            orig(self);
            LevelStateObjectReferences.dict["Viking_BlackSpearman"] = vr;
        };
    }
}
```

| 评估项 | 评分 |
|--------|------|
| DLL 代码量 | ~30 行（仅 AssetBundle 加载 + dict 注册） |
| 核心工作量 | 转移到 Unity Editor 中预制 prefab |
| 优势 | prefab 完整（含 Mesh/Material/Animator），质量最高 |
| **结论** | ✅ **最佳「外置+轻型DLL」折中方案** |

---

## 5. 方案对比总览

| 维度 | 路径A：克隆注入 | 路径B：IL枚举扩展 | C1：Python内存 | C2：资源替换 | C3：AssetBundle |
|------|:---:|:---:|:---:|:---:|:---:|
| **实现难度** | ⭐⭐ | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐ | ⭐⭐⭐ |
| **需要DLL** | 是（~200行） | 是（大量Patch） | 否 | 否 | 是（~30行） |
| **需要外部工具** | 否 | MonoMod | pymem | UABE | Unity Editor |
| **独立枚举值** | ❌（复用） | ✅ | ❌ | ❌ | ❌ |
| **完整3D模型** | ❌（借用） | ❌ | ❌ | ✅ | ✅ |
| **独立概率控制** | ✅ | ✅ | ✅ | ✅ | ✅ |
| **稳定性** | ⭐⭐⭐⭐⭐ | ⭐⭐ | ⭐ | ⭐⭐ | ⭐⭐⭐⭐ |
| **推荐度** | ⭐⭐⭐⭐⭐ | ⭐ | ⭐ | ⭐⭐ | ⭐⭐⭐⭐ |

---

## 6. 最终推荐路径

### 首推：路径 C3（AssetBundle + 极简DLL）

如果你能在 Unity Editor 中制作包含完整 Mesh/Material/Animator 的 prefab 并打包为 AssetBundle，**路径 C3 是质量最高的方案**。

### 备选：路径 A（纯 DLL 克隆注入）

如果无法使用 Unity Editor 制作 AssetBundle，**路径 A 是当前最实际、最稳定的方案**。

---

## 7. 针对 v1.1 的具体落地建议

| 当前 v1.1 | 改为 |
|-----------|------|
| `ModVR()` 直接修改 dict 中的 `Viking_SwordShield` | 创建新 GameObject → Instantiate 克隆 → 以新名字注册 |
| 所有 SwordShield 变成黑矛兵 | 黑矛兵和 SwordShield 独立并存 |
| `BlackSpearmanMarker` 仅做标记 | `BlackSpearmanMarker` 同时用于扫描识别 |

### 需要新增/修改的文件

```
BadNorthBlackSpearman1.1/
├── Plugin.cs                    ← 重写为「克隆注册 + 定时扫描 + Harmony Patch」
├── SpearChargeComponent.cs      ← 保留（冲刺技能）
├── SpearStabAction.cs           ← 保留（刺击技能）
└── BadNorthBlackSpearman1.1.csproj
```

---

## 8. 实施检查清单

- [ ] `Viking_SwordShield` 在 dict 中存在（启动日志验证）
- [ ] `Instantiate(sourceVR.gameObject)` 成功克隆所有组件
- [ ] 新 VR 成功注册到 `LevelStateObjectReferences.dict`
- [ ] `LevelRule.condition` 表达式语法正确（fraction 范围）
- [ ] `LevelGuessable.probability` 设置生效（敌舰中出现黑矛兵）
- [ ] Agent 生成后 `BlackSpearmanMarker` 被正确识别
- [ ] SpearChargeComponent / SpearStabAction IBrainAction 注入
- [ ] GetAttack / range Harmony Patch 生效
- [ ] 原版 SwordShield 仍然正常出现（未被劫持）

---

## 9. 总结

| 问题 | 答案 |
|------|------|
| **能用非 DLL 方式吗？** | 理论上可以，但都比 DLL 方式更复杂、更不稳定 |
| **DLL 方案的核心难点是什么？** | 不是 DLL 本身难，而是 ①枚举值不可扩展 ②完整预制件无法从零创建 ③武器替换的渲染管线复杂 |
| **最佳实际方案？** | **路径A（克隆注入法）** — 复用现有 prefab，以新名字注册，通过数值/颜色/AI行为实现差异化 |
| **为什么放弃武器替换？** | 从 Pikeman 提取 BatchedSprite 武器的复杂度远超其带来的视觉效果 |
| **能否完全复刻我方长矛兵？** | 不能 — Pikeman 的 Spear Brain、spearAnim BatchedSprite、SpriteAnimator 绑定在运行时极难迁移到敌方单位 |

---



---

## 10. 🚀 突破 DLL 边界 —— 五种「非传统」方案

> 你游戏目录中已经存在的关键资源：
> - `BepInEx/core/Mono.Cecil.dll` ✅ 已就位 — IL 字节码工程库
> - `BepInEx/patchers/` ✅ 已就位 — 预加载补丁目录
> - `BadNorth_Data/data.unity3d` — Unity 主资源包
> - `BadNorth_Data/resources.resource` — 资源文件
> - `BadNorth_Data/sharedassets1.resource` — 共享资源文件

### 10.1 方案 D：Mono.Cecil 预加载补丁（BepInEx Preloader Patcher）⭐ 新思路

这**不是**普通的 BepInEx 插件 DLL，而是一个在游戏加载之前运行的**IL 字节码补丁**：

```
游戏启动
  ↓
BepInEx Preloader 初始化
  ↓
加载 BepInEx/patchers/*.dll ← ⭐ 我们的 Patcher 在这里运行
  ↓
Patcher 使用 Mono.Cecil 直接修改 Assembly-CSharp.dll 的 IL 代码：
  ① 向 VikingAgent.Type 枚举添加 BlackSpearman = 8
  ② 修改 numTypes 从 8 → 9
  ③ 修改所有 switch(type) 的分发逻辑
  ④ 可选：向 GameSetup 注入预制件注册代码
  ↓
Unity 引擎启动 → 加载修改后的 Assembly-CSharp
  ↓
游戏中：BlackSpearman 是「原生」枚举值！
```

**优势**：
- ✅ 在 IL 层面「永久」修改了游戏代码，不受运行时反射限制
- ✅ 枚举值变成真正的 `VikingAgent.Type.BlackSpearman`
- ✅ Mono.Cecil.dll 已经在你的 `BepInEx/core/` 中
- ✅ 代码量很少 — 典型 Patcher 约 100-150 行
- ✅ 无需 Unity Editor、无需 AssetBundle

**限制**：
- ⚠️ 仍需要一个轻量运行时 DLL 来处理 prefab 克隆和 AI 注入
- ⚠️ 需要学习 Mono.Cecil API（但比 MonoMod 简单得多）

### 10.2 方案 E：Unity 资源文件直接修改（Asset Patcher）⭐ 新思路

Bad North 的敌人 prefab 存储在 `data.unity3d` / `sharedassets1.resource` 这些 Unity 资源包中。可以用 Python 的 **UnityPy** 库直接操作：

```python
# 概念代码 — UnityPy
import UnityPy

# 1. 打开游戏资源包
env = UnityPy.load("BadNorth_Data/sharedassets1.resource")

# 2. 遍历所有 GameObject，找到 Viking_SwordShield
for obj in env.objects:
    if obj.type.name == "GameObject" and obj.name == "Viking_SwordShield":
        # 3. 克隆 → 重命名 → 修改 VikingReference.type → 保存
        cloned = obj.clone()
        cloned.name = "Viking_BlackSpearman"
        # ... 修改 serialized 字段 ...
        break

# 4. 写入修改后的资源包
with open("BadNorth_Data/sharedassets1_patched.resource", "wb") as f:
    env.save(f)
```

**优势**：
- ✅ **完全不需要任何 DLL 运行时**（如果只是替换资源）
- ✅ 可以利用 Unity Editor 的 prefab 完整性
- ✅ Python 工具成熟

**限制**：
- ⚠️ 枚举值限制仍然存在（需要用方案 D 配合）
- ⚠️ UnityPy 对 Unity 2018 版本的兼容性需验证
- ⚠️ 每次游戏更新需要重新 patch

### 10.3 方案 F：独立控制台 Patcher（脱离 BepInEx）


---

## 13. 🔬 D + F 深度拆解 —— 从原理到代码

### 13.1 全局架构图

```
┌──────────────────────────────────────────────────────────────────┐
│                     一次性 Patch 流程                              │
│                                                                  │
│  ┌─────────────────────┐     ┌─────────────────────┐              │
│  │  EnumPatcher.exe    │     │  AssetPatcher.py    │              │
│  │  (C# + Mono.Cecil)  │     │  (Python + UnityPy) │              │
│  │                     │     │                     │              │
│  │  输入:              │     │  输入:               │              │
│  │  Assembly-CSharp.dll│     │  data.unity3d       │              │
│  │                     │     │                     │              │
│  │  操作:              │     │  操作:               │              │
│  │  ① 加载 DLL 到内存  │     │  ① 解析 Unity 资源包 │              │
│  │  ② 定位枚举定义     │     │  ② 找到 SwordShield  │              │
│  │  ③ 追加新枚举值     │     │  ③ 深度克隆 prefab   │              │
│  │  ④ 修改 numTypes    │     │  ④ 改 type/bounty    │              │
│  │  ⑤ 保存 .dll        │     │  ⑤ 保存资源包        │              │
│  │                     │     │                     │              │
│  │  输出:              │     │  输出:               │              │
│  │  Assembly-CSharp.dll│     │  data.unity3d        │              │
│  │  (已备份原文件)      │     │  (已备份原文件)       │              │
│  └─────────────────────┘     └─────────────────────┘              │
│            │                           │                          │
│            └─────────┬─────────────────┘                          │
│                      ▼                                            │
│             LaunchModded.bat                                      │


### 13.2 Part D 深度解析：EnumPatcher.exe

#### 核心原理 —— 为什么不只是"加一个整数"

传统 BepInEx 插件中，你用反射创建一个"枚举值"时，它只是一个裸 `int`。`Enum.GetValues(typeof(VikingAgent.Type))` 仍然只返回 8 个值，因为 CLR 的枚举元数据中没有你的新值。

**Mono.Cecil 不同**：它在 IL 二进制层面往 `Assembly-CSharp.dll` 的元数据表中写入一个新的 `.field public static literal` 字段。修改后的 DLL 被 Mono 运行时加载时，`Enum.GetValues` 会扫描所有 `literal` 字段并返回 9 个结果——你的 `BlackSpearman` 是真正的、一等公民的枚举值。

#### 目标 IL 结构

原版 `VikingAgent.Type` 在 IL 中：
```
.field public static literal ... Sword       = int32(0)
.field public static literal ... SwordShield = int32(1)
...
.field public static literal ... Twohanded   = int32(7)
```

我们需要新增：
```
.field public static literal ... BlackSpearman = int32(8)
```

#### 核心代码骨架（完整代码见项目 Tools/EnumPatcher/）

```
EnumPatcher.exe 的核心流程：

1. AssemblyDefinition.ReadAssembly(dllPath)   ← 加载 DLL
2. 遍历 module.Types → 找到 "VikingAgent" → 找到嵌套 "Type"
3. new FieldDefinition("BlackSpearman", Public|Static|Literal|HasDefault, enumType)
4. newField.Constant = 8;  enumType.Fields.Add(newField);
5. assembly.Write()  ← 保存

总共 ~60 行有效代码。
```

#### numTypes 自动更新

原版 `numTypes`：
```csharp
public static readonly int numTypes = Enum.GetValues(typeof(Type)).Length;
```

这是静态构造函数计算的。新增枚举字面量后，`Enum.GetValues` 返回值长度自动从 8 变 9。**不需要手动修改任何代码**。

#### switch 语句安全性

当代码中有 `switch(type)` 时，IL 中表现为 `switch` 指令 + 跳转表（大小为最大 case 值 + 1）。传入新值 8 且跳转表只覆盖 0-7 时，CLR 会 fall through 到 `default` 标签。原版代码的 default 通常做安全兜底（当作 Sword 处理），所以**不会崩溃**。

#### 编译 & 运行

```powershell
copy BepInEx\core\Mono.Cecil.dll .
csc /r:Mono.Cecil.dll /out:EnumPatcher.exe EnumPatcher.cs
EnumPatcher.exe "BadNorth_Data\Managed\Assembly-CSharp.dll"

# 输出:
# [INFO] Backed up to: ...dll.orig_backup
# [INFO] Found: Voxels.TowerDefense.VikingAgent/Type
# [INFO] Added: BlackSpearman = 8
# [SUCCESS] Patched
```

│             (恢复备份 → 运行 Patcher → 启动游戏)                   │
└──────────────────────────────────────────────────────────────────┘
```


写一个独立的 `.exe` 程序，在游戏启动前运行：


### 13.3 Part F 深度解析：AssetPatcher.py

#### 13.3.1 Bad North 的资源文件布局

| 文件 | 内容 |
|------|------|
| `BadNorth_Data/data.unity3d` | 主资源包 — 场景、prefab、Material、Texture |
| `BadNorth_Data/sharedassets1.resource` | 共享资源 — 部分 prefab、音效 |
| `BadNorth_Data/resources.resource` | 动态加载资源 |

VikingReference 所在的 prefab 最可能在 `data.unity3d` 中。建议先用 **AssetStudio** (GUI) 探索确认。

#### 13.3.2 Unity 序列化回顾 —— 为什么用 UnityPy 可以改 prefab

一个 `VikingReference` MonoBehaviour 在资源包中的序列化形式大致是：

```
GameObject "Viking_SwordShield"
  ├── Transform
  ├── VikingReference (MonoBehaviour)
  │     ├── viking: {fileID: xxx}     ← 指向 VikingAgent 子 prefab 的 PPtr
  │     ├── type: 1                    ← SwordShield (int32)
  │     ├── bounty: 5                  ← int32
  │     ├── sprite2: {fileID: yyy}     ← Sprite 纹理引用
  │     ├── approachAudioId: "Mus/..."
  │     └── arriveAudioId: "Mus/..."
  ├── LevelRule (MonoBehaviour)
  │     └── condition.expression: "..."
  └── LevelGuessable (MonoBehaviour)
        └── probability: {...}
```

克隆后只需修改：
- `m_Name`: `"Viking_BlackSpearman"`
- `type`: `1 → 8`
- `bounty`: `5 → 8`
- `LevelRule.condition.expression`: 改为出现条件
- `LevelGuessable.probability`: 复制参考值

**关键**：`viking` 字段的 PPtr 引用保持不变（仍指向原来的 VikingAgent 子 prefab），因为我们复用同一个 3D 模型。

#### 13.3.3 操作步骤

```
前置：用 AssetStudio 确认
  1. 打开 AssetStudio GUI
  2. File → Load File → data.unity3d
  3. 搜索 "Viking_SwordShield"
  4. 确认所在的 assets 文件名
  5. 记录 MonoBehaviour 的 PathID

Python: 用 UnityPy 修改
  1. env = UnityPy.load(asset_file)
  2. 找到源 GameObject → 深度克隆
  3. 修改序列化字段
  4. env.save()
```

#### 13.3.4 概念 Python 代码

```python
import UnityPy
import shutil
from pathlib import Path

ASSET = Path("BadNorth_Data/data.unity3d")
SRC = "Viking_SwordShield"
DST = "Viking_BlackSpearman"

# 备份
if not Path(str(ASSET) + ".bak").exists():
    shutil.copy2(ASSET, str(ASSET) + ".bak")

# 加载
env = UnityPy.load(str(ASSET))

# 找到源 prefab 并克隆
for obj in env.objects:
    if obj.type.name == "GameObject":
        data = obj.read()


### 13.4 D + F 工具链协作流程

```
LaunchModded.bat
      │
      ├── 检查 .orig_backup 是否存在
      │     │
      │     ├── 不存在 → 创建备份 + 运行 Patcher
      │     └── 存在   → 从备份恢复 + 重新 Patch（可选）
      │
      ├── [D] EnumPatcher.exe Assembly-CSharp.dll
      │     └── 添加 BlackSpearman = 8 → 保存
      │
      ├── [F] python AssetPatcher.py data.unity3d
      │     └── 克隆 Viking_SwordShield → Viking_BlackSpearman
      │
      └── 启动 BadNorth.exe
            │
            ├── Mono 运行时加载 修改后的 Assembly-CSharp.dll
            │     └── VikingAgent.Type 现在有 9 个值 ✓
            │
            ├── Unity 加载 修改后的 data.unity3d
            │     └── 场景中包含 Viking_BlackSpearman prefab ✓
            │
            └── GameSetup.Awake()
                  └── IGameSetup.OnGameAwake()
                       └── Viking_BlackSpearman 注册到 dict ✓
                            └── type = BlackSpearman (8) ✓
```

### 13.5 为什么 D + F 从根本上解决了问题

| 传统 DLL 插件 | D + F 工具链 |
|:---|:---|
| `Type` 枚举值 = 8 个 | `Type` 枚举值 = **9 个**（真·编译级） |
| `newVR.type = SwordShield`（冒充） | `newVR.type = BlackSpearman`（原生值） |
| 反射修改 `numTypes` 可能失败 | `Enum.GetValues` 自动返回 9 |
| UI 中类型显示 = SwordShield | UI 可区分 BlackSpearman |
| 每次启动都要执行注入逻辑 | **Patch 一次，永久生效** |
| 启动时约 200 行运行时代码 | 启动时零额外开销 |
| 组件注入需定时扫描 | 预制件上直接有正确的组件 |

### 13.6 可选的迷你 BepInEx 插件（仅用于自定义 AI 技能）

D + F 解决了「单位类型注册 + prefab 创建」。如果需要 SpearCharge / SpearStab 等 AI 技能，只需一个 ~40 行的轻量插件：

```csharp
[BepInPlugin("bs.ai", "BlackSpearman AI", "1.0")]
public class BlackSpearmanAI : BaseUnityPlugin
{
    static HashSet<Agent> _done = new HashSet<Agent>();
    
    void Awake()
    {
        // Hook Landing.Spawn 来注入技能
        On.Voxels.TowerDefense.RaidGeneration.Landing.Spawn +=
            (orig, self) => {
                var ship = orig(self);
                foreach (var a in ship.agents)
                {
                    var va = a.GetComponent<VikingAgent>();
                    if (va != null && va.type == (VikingAgent.Type)8 && _done.Add(a))
                    {
                        a.gameObject.AddComponent<SpearChargeComponent>()
                            .Setup(a);
                        a.gameObject.AddComponent<SpearStabAction>();
                    }
                }
                return ship;
            };
    }
}
```

这个插件只有 ~25 行有效代码，因为它不用处理枚举限制、不用克隆 prefab、不用修改 dict —— 这些都被 D + F 在启动前解决了。

### 13.7 项目文件结构

```
BadNorthEnemy-main/
├── 新增敌方单位技术方案.md
├── Tools/
│   ├── EnumPatcher/
│   │   ├── EnumPatcher.cs          (C# 源码)
│   │   ├── EnumPatcher.csproj      (.NET 4.7.2)
│   │   └── Mono.Cecil.dll          (从 BepInEx/core/ 复制)
│   ├── AssetPatcher/
│   │   ├── asset_patcher.py        (Python 脚本)
│   │   └── requirements.txt        (UnityPy>=1.9.0)
│   └── LaunchModded.bat            (一键脚本)
└── MiniPlugin/
    ├── BlackSpearmanAI.cs          (~25 行)
    └── BlackSpearmanAI.csproj
```

### 13.8 LaunchModded.bat

```batch
@echo off
set GAME_DIR=D:\Steam\steamapps\common\BadNorth
set DLL=%GAME_DIR%\BadNorth_Data\Managed\Assembly-CSharp.dll
set ASSET=%GAME_DIR%\BadNorth_Data\data.unity3d

echo ======================================
echo Bad North - D+F Launcher
echo ======================================

REM --- Part D: Enum Patch ---
if not exist "%DLL%.orig_backup" (
    echo [D] Running EnumPatcher...
    Tools\EnumPatcher\EnumPatcher.exe "%DLL%"
    if !ERRORLEVEL! neq 0 (
        echo [ERROR] EnumPatcher failed!
        pause & exit /b 1
    )
) else ( echo [D] DLL already patched )

REM --- Part F: Asset Patch ---
if not exist "%ASSET%.orig_backup" (
    echo [F] Running AssetPatcher...
    python Tools\AssetPatcher\asset_patcher.py "%ASSET%"
    if !ERRORLEVEL! neq 0 (
        echo [ERROR] AssetPatcher failed!
        pause & exit /b 2
    )
) else ( echo [F] Assets already patched )

echo.
echo All patches applied. Launching game...
start "" "%GAME_DIR%\BadNorth.exe"
```

        if data.name == SRC:
            # UnityPy 允许通过 assets_file 创建新对象
            # 具体 API 因版本而异，核心逻辑如上所述
            new_go = obj.assets_file.create_object("GameObject", DST, data)
            print(f"[OK] Cloned: {SRC} → {DST}")
            break

# 保存
env.save()
print("[DONE] Asset patched")
```

> ⚠️ 完整可运行版本需要针对 Unity 2018.x 的序列化格式做适配。建议先用 AssetStudio 确认 VikingReference 的具体文件位置和字段布局后再写完整脚本。


```
LaunchBadNorthModded.bat:
  ① EnumPatcher.exe           ← 用 Mono.Cecil 修改 Assembly-CSharp.dll
  ② AssetPatcher.py            ← 用 UnityPy 修改 sharedassets1.resource
  ③ BadNorth.exe               ← 启动游戏
```

**第③步启动的游戏完全不需要 BepInEx！**（如果你只需要新单位出现，不需要自定义 AI 技能的话）

如果你还需要自定义 AI 行为（冲刺、刺击），可以配合一个极小的 BepInEx 插件。

### 10.4 方案 G：存档注入法（Save File Modding）

Bad North 使用 Unity 的序列化系统保存战役进度。存档文件在：
`%USERPROFILE%\AppData\LocalLow\PlausibleConcept\BadNorth\`

存档中包含每关的 `LevelState` — 其中 `objectReferences` 列表决定了本关有哪些敌人种类。

**思路**：在存档中直接插入 `Viking_BlackSpearman` 的引用，跳过关卡生成时的 ArcConsistency 筛选。

**优势**：
- ✅ 完全不需要任何 DLL
- ✅ 可以做存档编辑器（Python/JS GUI 工具）

**限制**：
- ❌ 存档是二进制格式，逆向工程工作量大
- ❌ 仍需要 prefab 实际存在

### 10.5 方案 H：Mono 运行时劫持（Native Proxy DLL）

将自定义的 `version.dll` 或 `winhttp.dll` 放入游戏目录。Windows 会优先加载同目录的 DLL（DLL 劫持）。这个 proxy DLL 可以在 Mono 运行时级别注入 C# 代码。

**优势**：
- ✅ 极高的控制权 — 可以劫持任何 Mono 内部 API
- ✅ 不需要 BepInEx

**限制**：
- ❌ 需要 C/C++ 和 Mono 嵌入式 API 知识
- ❌ 防病毒软件可能误报

---

## 11. 方案组合矩阵 — 突破边界的正确姿势

| 组合 | Enums | Prefabs | AI技能 | BepInEx依赖 | 复杂度 |
|------|:---:|:---:|:---:|:---:|:---:|
| **D+运行时DLL** | Mono.Cecil Patcher | 克隆注入 | Harmony Patch | 需要 | ⭐⭐⭐ |
| **D+F（完全外置）** | Cecil Patcher.exe | UnityPy Patcher | 不需要 | 无 | ⭐⭐⭐ |
| **D+F+迷你DLL** | Cecil Patcher.exe | UnityPy Patcher | 迷你BepInEx | 需要 | ⭐⭐⭐⭐ |
| **H 独立** | Native Proxy | 反射创建 | Native Mono API | 无 | ⭐⭐⭐⭐⭐ |

### 推荐：D+F+迷你DLL（完整独立新单位）

```
工具链：
┌─────────────────────────────────────────────────────┐
│  EnumPatcher.exe (C# + Mono.Cecil)                  │
│  ├─ 加载 Assembly-CSharp.dll                        │
│  ├─ 向 VikingAgent/Type 添加 BlackSpearman = 8      │
│  ├─ 修改 numTypes = 9                               │
│  └─ 保存 → Assembly-CSharp_patched.dll              │
├─────────────────────────────────────────────────────┤
│  AssetPatcher.py (Python + UnityPy)                 │
│  ├─ 加载 sharedassets1.resource                     │
│  ├─ 克隆 Viking_SwordShield → Viking_BlackSpearman  │
│  ├─ 设置 VikingReference.type = 8 (BlackSpearman)   │
│  └─ 保存 → sharedassets1_patched.resource           │
├─────────────────────────────────────────────────────┤
│  LaunchModded.bat                                   │
│  ├─ 检查并应用 patch（如果尚未应用）                 │
│  └─ 启动 BadNorth.exe                               │
└─────────────────────────────────────────────────────┘
```

**关键突破**：加上枚举 Patcher 后，`VikingAgent.Type.BlackSpearman` 变成了编译级的存在。预制件上的 `type = 8` 可以直接被序列化系统识别，不需要任何反射 hack！

---

## 12. 总结更新

| 维度 | 传统DLL插件 | 新方案：Preloader Patcher | 新方案：完全外置工具链 |
|------|:---:|:---:|:---:|
| **能新增枚举值** | ❌ | ✅ (Mono.Cecil) | ✅ (Mono.Cecil) |
| **完整预制件** | ❌ (反射克隆) | ⚠️ (仍需克隆) | ✅ (UnityPy编辑) |
| **运行时DLL** | 必须 | 需要（小型） | 可选 |
| **BepInEx依赖** | 强依赖 | 仅作Patcher宿主 | 可选 |
| **可分发性** | 放入BepInEx/plugins | 放入BepInEx/patchers | 独立exe+py |
| **维护成本** | 中 | 中 | 低（一次patch永久生效） |

> **核心洞见**：你的项目目录中已经躺着一个 `BepInEx/core/Mono.Cecil.dll` 和一个空的 `BepInEx/patchers/` 目录。这套 IL 补丁基础设施是 BepInEx 内置的，但几乎没人用。它可以让你绕过所有运行时反射的限制，直接在 IL 层面「写死」新单位类型。**这才是突破 DLL 边界的真正答案。**


> **下一步**：选择路径 A 或 C3，我可以帮你直接实现完整代码。
